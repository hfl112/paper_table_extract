# 开发日志：2026-07-25 / 26

一个窗口内从空目录做到可用 CLI。记录**做了什么、测了什么、踩了什么坑、最后怎么选**。
稳定结论已固化进 `AGENTS.md`；技术路线在 `~/.claude/plans/luminous-roaming-teapot.md`。

---

## 0. 起点

- 目标：给一篇**英文**文献 PDF + 一组关键词 → 把命中的表格（含图片形式的表）导出成 CSV。
- 前身项目 `../02_extract_ref/` 号称"太复杂且不能移植"。**核实结果**：那目录只是六阶段流水线的
  **第 2 阶段**（LLM 语义抽取），真正做 PDF→表格的 Stage 01 **已不在机器上**；代码硬编码了一堆
  不存在的兄弟目录，压根不吃 PDF、只吃上游 JSON。→ 从零写，只继承其三份 handoff 的教训。
- 从 handoff 继承的两条实测记录：
  - `pbc_28772 Table 1` 在前身项目里是**弱表**（"A 错行、B 转置都不理想，保留 A"）。本项目用
    docling+转正一次得到正确 44 行，**比当初好**，所以它才能当金标准。
  - 同页多表时版面模型会选错（"少数 Table I 块实际指向 Table II"）。本项目遇到同类 bug（见 §5）。

---

## 1. 技术路线被实测推翻 3 次

**这是本项目最重要的过程事实** —— 也是 `AGENTS.md` 里"技术路线不写在这里"那条规矩的来由。

| # | 我的判断 | 推翻它的证据 |
|---|---|---|
| 1 | "docling 一趟就能搞定全部" | 它把"长得像表的图"归成 `PictureItem`，那种表**压根不在** `TableItem` 列表里 |
| 2 | "确定性几何为主，docling 兜底" | `pdfplumber` lattice 与 PyMuPDF `find_tables()` 在 `pbc_28772`/`pbc_29304` 上返回 **0** 张表；`pbc_21078` 真表在 p4/p5/p6/p8，两者只在 p10/p11 命中（误报）。根因：三线表无竖线。旋转 0/90/270 都试过，救不回来 |
| 3 | "只匹配 caption+legend"（用户据我给的选项拍板） | 我把"匹配表内内容"整体判成昂贵操作，**这对文字表是错的** —— docling 整篇跑一次、过滤在抽取之后，内容早在内存里。代价：金标准表 caption 无 `response`，第 11 列表头是 `Obj. Response`，`-k response` 抓不到 |

第 3 条尤其值得记：**我让用户在一个假的取舍里做了选择。** 修正后 `-k response` 在 8 篇上从 1 张变 11 张，其中 8 张靠 `matched_on=cell`。

---

## 2. 探针（决定技术路线，脚本在 `spike/`）

| 问题 | 结论 |
|---|---|
| docling 能否当主干 | **能**。`pbc_28772` 抽出 `38×11`+`6×11(Continued)` = **44 行**，命中前身项目金标准 |
| figure 里的表在哪 | 在 `PictureItem`。CCR-18-2728 有 8 个表标号，docling 只给 1 个 `TableItem` |
| caption vs 正文引用怎么分 | **标号位于某一「行」开头**，6/6 篇与 docling 结果吻合。不能用"block 开头"（PyMuPDF 会把两个 caption 并进一个 block，blood p6 的 `Table 3` 偏移 931）；标号后必须允许**行尾**（`pbc_28772` 的 caption 行只有 `TABLE 1` 三个字） |
| 横向表阈值 | 竖排行数 ≥30。真横向表 134–446 行、Wiley 水印恒 1 行。**必须绝对行数**（`pbc_21078` p7 图片页 1 行水印占比就 12.5%） |
| 转正方向 | `rot=90` 全对，`0`/`270` 全错（4/4）。抢救量：`pbc_29304` 从 `15×16+1×2` → `17×15+24×15`；`pbc_21078` 从 `9×3+8×2` → `43×9+16×8` |
| 粗 OCR 成本 | 10.6–28.4 秒/图，**比 docling 跑完整篇（8–14 秒）还贵**，且 8 张图只 1 张含目标词 |
| 图片表还原 | 一开始全失败，两道修正救活：绕过版面分类用 `TableCellsDetection`（整图被判 `chart`(0.79)）；强制英文识别模型（`lang='en'` 不被接受，默认模型把 `-3` 认成中文 `心`） |
| supp 标号 | 只被引用、没有真 caption ⇒ **实体不在本 PDF**（CCR 有 13 个）→ 报 `label_referenced_but_absent` |

**开工前必验 3 项**（都通过，避免了架构返工）：
1. 整篇归一化 PDF 喂 docling → 与逐页转正**逐字一致**（省十几倍时间）
2. `TableCellsDetection` 当闸门 → **0.4–0.7 秒/图**，判别力 300 vs 0–6 格
3. 出版社 OCR 文字层的页 → docling **能**从字符坐标恢复行列结构（`Male + female|49 + 18|21 + 5` 正确切 3 列）→ 不需特例分支

---

## 3. 实现

8 个文件，原则是**一个文件独占一个第三方库**（三个引擎都可能被换，今天已换过一个）：

`models.py`(零依赖) `rules.py`(零依赖，全部判据) `pdfio.py`(PyMuPDF) `engine_docling.py`
`engine_plumber.py`(只报列数当第二意见) `engine_paddle.py` `emit.py` `__main__.py`

**42 个单测**（`rules.py`/`models.py` 零依赖，0.06s 跑完），每个 case 都来自真实文献的实测值。

---

## 4. 分流架构（核心判据）

**给定候选区域，数框内文字层字符数**：≈0 → 内容是像素 → OCR 路径；有字 → 精确抽取路径。
实测分离极干净：三张位图框内**全是 0**，所在页有 1480–4202 字符。

这个判据**自动涵盖矢量表 vs 位图表**，无需专门区分 —— 矢量表的线条是矢量指令但文字在文字层。

---

## 5. 回归与用户人眼核对逼出的 bug

### 回归自己发现的（3 个）
| bug | 表现 | 修法 |
|---|---|---|
| figure 图注被配给 table | blood p4/p6 上 `Table 2`/`Table 3` 被标成 `fig_1`/`fig_4`（同页有两种 caption） | 配对**按 kind 分开、table 优先** |
| caption 归属校验误报 | `TABLE I. Tumor Demographics` 被判"归属可疑"（表头是 `Xenograft line\|Histology`） | 判据从"必须有重叠"改成**相对比较**：只有另一个**同类** caption 与网格重叠 ≥2 词才算反证。**缺少证据 ≠ 有反证** |
| manifest 刷屏 | blood 有 18 行"未导出"，13 行是 logo 和多面板小图 | logo 按页高占比 8% 滤（实测 logo 3.9%/1.9%/3.5% vs 真内容 13.2%）；其余在摘要里聚合、明细仍进 manifest |

### 用户 ground truth 逼出的（3 个，全是**静默漏表**）
| bug | 根因 | 修法 |
|---|---|---|
| 矢量图整体漏掉（`pbc_21078` Fig.3/4） | 图片候选来源是"嵌入位图"，那页**位图数 0**、矢量指令 105/112 条；docling 又归成 Picture → 两条路都看不见 | 候选来源改成「**图区域**」（docling `PictureItem`）：有位图抠原生分辨率、无位图**渲染**（与前身项目 Channel B 一致） |
| 裁剪框被撑到整图（`pbc_21296` Fig.1） | 检测器在条形图区域也检出伪单元格。实测每 5% 宽度格数 `[47,0,0,48,0,47,47,0,47,47 \| 0,0,0,1,0,0,0,1,0,0]`，外接框占宽 **99%** = 等于没裁 | 裁剪框**只用密集列带**算 |
| 同一张表抽两遍 | docling 把一张位图 `[127,77,463,301]` 拆成两个 `PictureItem`（左半表、右半图），我却每次抠整图 | **按子区域裁剪原生位图** —— 上游已分好区，比密集带启发式更准 |

**这三个 bug 是同一个模式：上游已经给了正确信息，我却没用。**

外加改进：**低分辨率图按有效 dpi 自动放大**（目标 300dpi、最多 4×）。实测**缺一不可** ——
只放大不裁剪 1×/2×/3×/4× 全失败；只裁剪不放大出 `44×5`、表头全空、`-2.0` 读成 `-210`；
两者都做 → `45×6`、`-2.0` 正确。

---

## 6. 关键词语义大改

### 从"子串"改成"全词 + 英文屈折变体"
| 关键词 | 子串命中 | 新规则 |
|---|---|---|
| `PR` | **130**（`rep`**r**`esentative`/`P`**r**`exasertib`/`P`**r**`otein`） | 8 |
| `CR` | **41**（大量来自 `vin`**cr**`istine`） | 2 |
| `xenograft` | 38 | 38（`xenografts` 靠屈折保住） |
| `Demographic` | 2 | 2（纯全词是 **0** —— 原文是 `Demographics`） |

**两件事必须同时做**：只改全词会让 `Demographic` 命中 0 张。覆盖的形态：`+s/+es`、`y→ies`
（`toxicity`→`toxicities`）、`+d/+ed/+ing`、`e→ing`（`dose`→`dosing`）、末辅音双写
（`control`→`controlled`）。含数字的词（`PD1`）不做形态展开。

### 连字符：我先选反了，被数据纠正
- 初版理由："`dose-response` 在药理文献极常见，漏它比误命中 `PD-L1` 更糟" → 选了"连字符算边界"
- 用户一句"`dose-response curves` 是图吧，我们不需要"点破，且**更根本的问题是我拿一个语料里不存在的形式论证**（原文是空格写法 `Dose response curves`）
- 实测所有连字符 token：`PR-104`/`PR-104A`（药名，blood 整篇讲它）、`NB-SD`（移植瘤系名）、
  `ALL-11`/`KT-13`/`CTG-0241`/`JNJ-26481585`/`BCP-ALL`（编号代号）—— **没有一个是值得抓的复合词**
- → 改成"连字符算词字符"：零召回损失 + 消掉一整类误报

### 表达式语言（`response+(PD|CR)*3`）—— 决定不做
用户提出后自己判断"适合后面再处理"，理由成立并已记档：**阈值放下游对 CSV 做更准**（数单元格是
精确计数，在文本 blob 上数是近似猜测）。同理响应码 `PD/CR/PR/SD/MCR` 也不加进 `response` 组 ——
**拒绝理由是选择性不够，不是技术做不到**（技术障碍已被全词匹配消除）。

---

## 7. legend 跨 block 被截断

`pbc_30017` p7：`block2` = `Table 1:` + 标题；`block3` = 整段描述（含 `objective response measure`
/`osteosarcoma models`/`PD1`）**被完全漏掉**，legend 只有 60 字。

判据：**间距 <15pt 且最长行 ≥40 字符**才续抓。四个案例各自单独用都会错一个：

| 案例 | 间距 | 最长行 | 谁拦住的 |
|---|---|---|---|
| `pbc_30017` legend 续段 | 8.7 | 107 | 吸收 |
| `pbc_30017` 表体 `Cancer Type` | 23.0 | 11 | 间距 |
| `blood` p6 表体 `Treatment` | **4.2（很小）** | 14 | **只看间距会误吸**，靠行长 |
| `pbc_24724` p5 页脚 | 291.8 | 36 | 间距 |

踩坑：`pbc_30017` 是 PMC Author Manuscript 版，左边距有竖排 `Author Manuscript` 水印 block
（y 跨 108–712），在 y 序上插在 caption 与续段之间 —— 遇到"横向不重叠"就 `break` 会永远抓不到，改成跳过。

---

## 8. 留出集泛化检验（**抓到 3 个泛化 bug**）

12 篇料同时用于开发和验收 = 教科书式过拟合暴露面。用 `Zotero_Precancer_PDFs` 里
**5 篇从未参与调参**的现代论文检验，当场抓到 3 个：

| bug | 我原来只见过 | 留出集补上的**中间档** | 修法 |
|---|---|---|---|
| 转正误报（Becker 2022 p4/p6） | 水印 1 行 ↔ 真横向表 134–446 行 | **多面板图 Y 轴标签 41–79 行、占比 17–25%**（`UMAP dimension 2`/`PC2 (7.0%)`） | 绝对行数 **且** 占比 ≥50% |
| preprint 误报（Baslan 2022） | 真 preprint 总次数 378 | **正式版恰好 5 次**：Nature 标准栏目 `peer review information` + 参考文献 `Preprint at bioRxiv...` | 改看**页覆盖率**（84% vs 6%） |
| 扫描件误报（Beddows 2023 p24–29） | 真扫描件文字层 2163–6398 字/页 | **整版大图页仅 185 字/页**（只是页眉） | 整页位图 **且** 文字层 ≥1000 字 |

**每次都是同一个模式：我只见过两个极端，阈值落进了我没见过的中间档。**
第三个尤其危险 —— 误判为"扫描件"的页会被**跳过图片候选**，整版大图里的表**静默漏掉**。

---

## 9. 事故：AGENTS.md 被脚本炸毁并恢复

- **原因**：用 `python -c` 改文档，`s[s.index(A):s.index(B)]` 的两个锚点在文件里**顺序颠倒** →
  切片为空字符串 → `s.replace("", new)` 在 Python 里会**在每个字符之间插入一次** →
  文件从 323 行炸成 **192,753 行**。
- **恢复**：操作可逆（原字符还在，只是被交错插入）。扫描候选长度，以"按它切分应得到全是长度 1
  的碎片"为判据，精确定位插入单元 490 字符，还原出 19,242 字符 / 323 行，14 个章节全部完整。
- **教训**：文档编辑一律用 Edit 工具（匹配不到会**报错**），不要用脚本 `s.replace()`
  —— 后者在锚点失效时**静默**执行破坏性操作。**本项目没有 git**，无版本可回退。

---

## 10. 最终验收：与用户人眼核对 100% 一致

| PDF | Response（用户 → 工具） | PDX（用户 → 工具） |
|---|---|---|
| `pbc_21078` | Table 1,2,3+Fig 3,4 → **全中** | 同上 → **全中** |
| `pbc_24724` | Table 2+Fig 1 → 一致 | Figure 1 → 一致 |
| `pbc_28772` | Table 1 → ✅ | Table 1 → ✅ |
| `blood` | Table 1,2,3 → ✅＊ | Table 1,2,3 → ✅ |
| `pbc_21296` | Table 1+Fig 1 → 一致 | 同上 → 一致 |
| `pbc_30017` | Table 1 → ✅ | Table 1 → ✅＊ |
| `pbc_26870` | Table 1 → ✅ | Table 1 → ✅＊ |
| `EAP gastric` / `rstl.1665` | NA → 正确拒跑/低置信 | 同 |

＊ = 需 `--keywords-file`（同义词是**用户的领域知识**，工具不内置）。

**最强反过拟合证据**：解决 `blood` Table 1 时推出的词表（`relapse`/`status`），**未加任何新词**
就让 `pbc_21078 TABLE I` 也命中 —— 那是另一篇论文的另一张表，同一个"人口学/临床状态表从不写
response"的模式。若我在迎合答案，会为它单独加词。

另：`pdf_table_extract/` 包代码里**没有任何按篇名/页码分支的逻辑**（25 处篇名全在注释里，
作为实测依据的出处）。

---

## 11. 阈值清单与余量（诚实分档）

| 余量宽（安全） | 观测值 |
|---|---|
| `TEXT_IN_REGION_THRESHOLD=30` | 0 vs 1036–3190 |
| `TABLE_CELLS_THRESHOLD=20` | 0–6 vs 99–300 |
| `VERTICAL_LINES_THRESHOLD=30` | 1 vs 41–446 |
| `SPARSE_BAND_RATIO=0.25` | 稀疏带 1 vs 密集带 47 |
| `OCR_LAYER_MIN_CHARS=1000` | 185 vs 2163–6398 |
| `PREPRINT_MIN_PAGE_RATIO=0.4` | 6% vs 84% |
| `VERTICAL_RATIO_THRESHOLD=0.5` | 17–25% vs 69–99% |

| **余量紧或单点拟合（真风险）** | 观测值 |
|---|---|
| `LEGEND_MIN_PROSE_LINE=40` | prose 107 vs 页脚 **36** ← 只差 4 |
| `LEGEND_MAX_BLOCK_GAP=15` | 8.7 vs 23.0 ← 1.5–1.7× |
| `CROP_PAD_RATIO=0.02` | **单数据点**（Heat Map 那列） |
| `MIN_RIVAL_OVERLAP=2` | 单数据点 |
| `MIN_IMAGE_COVER_H=0.08` | 3.9% vs 13.2% ← 约 2× |

---

## 12. 可复跑入口

```bash
conda activate gemini
conda run -n gemini python -m pytest tests/ -q       # 42 个单测，0.06s
bash eval/regression.sh                              # 端到端逐篇回归（每行可单独复制）+ 断言
bash eval/matrix.sh                                  # 三组关键词 × 12 篇 → 1.output/
bash holdout/run.sh                                 # 留出集趟一：25 篇 --list，检 4 个阈值判据
# （趟二已并入 eval，见 NEXT.md）                            # 留出集趟二：25 篇完整抽取 + 零标注判据
conda run -n gemini python eval/checks.py detect 1.output --pdf-dir 0.pdf_input
conda run -n gemini python eval/gold.py --validate-anchors 1.output
pdf-table-extract PAPER.pdf --list                   # 探索：列出全部标号 + 完整 caption/legend
```

---

# 2026-07-26（下午）：grill `NEXT.md` → 前提被推翻 → 重做 eval 层

## 13. `NEXT.md` 的问题定义是错的（四条实测证据）

`NEXT.md` 早上写的方案是"人工抄 3–5 张表的关键列当 gold，写 `score.py` 比对"。
grill 过程中用真实数据把它的**前提**逐条推翻，所以整份文件重写。

| # | `NEXT.md` 的判断 | 推翻它的证据 |
|---|---|---|
| 1 | "最大盲区是没验过格子里的字对不对" | 它举的三个证据（`<0.001`→`-0.001`、`Overal Croup`、`Rhabdold`）**全部来自图片表**，而图片表**已全部标着 `low`、已经举手**。局部问题被写成整体盲区 |
| 2 | 要人抄答案才有 ground truth | **文字表的 ground truth 免费，就在 PDF 里** —— 格子是从文字层**切**出来的、不是**认**出来的。按页逐字比：文字表 8 张 100% / 1 张 98.6%，图片表 24.6–56.1%，**中间空 42 个百分点** |
| 3 | （隐含）现有 `high` 表大概是对的 | 挖到一个活 bug：`pbc_21296 TABLE I` 第 41 行把两个数据行压成一格，而 manifest 是 `high`/`grid_status=ok`/`notes=''` —— **正是它自己排在危险度第 1 位的那种「没举手的错」** |
| 4 | "第一列当行锚点" | 在它自己挑的四张表上**全都不成立**：`pbc_28772` 第一列 8 行为空、`G401` 重复 8 次；`pbc_30017` 12 行全是 `Osteosarcoma`；`pbc_26870` `ES-4` 重复 16 次。§2.1 自己举的例子 `G401,PD`/`G401,PD2` 已经是重复键 |

**过程教训**：我一开始在 `NEXT.md` 的框架内提问（"红旗指标怎么定"、"锚点用哪列"），
用户一句"你过分依赖 next 的架构，根本没有实地考核他说的合理，还是假象"点破。
**实地考核只用了两条命令就把前提打穿了。**

## 14. 用户拍板的四条边界

1. **这一轮只做 eval，`pdf_table_extract/` 一行不动。** 理由：边 eval 边改会乱，而且 12 篇料
   同时用于开发和验收本来就是过拟合暴露面（§8），一边发现一边修会让 eval 失去独立衡量的作用。
   → 交付物是**问题清单**（`eval/findings.md`），修在下一轮统一做。
2. **`tests/` 与 `eval/` 拆开** —— 前者答"代码逻辑对不对"（pytest、0.06s、不碰 PDF），
   后者答"抽出来的表对不对"（真 PDF、分钟级、要 gold、可用 LLM）。
3. **LLM 当独立第二读者**减少人工审核，但**只能当检测器、不能写值**（铁律 #1）。
   评测脚手架允许调 API，CLI 本体维持"不联网"—— 已写进 `AGENTS.md` 非目标那节。
4. **零标注判据要在 12 篇开发集 + 25 篇留出集两边都跑** —— 留出集是唯一能区分
   "普遍缺陷"和"这 12 篇特有"的地方，而零标注判据是唯一能在那 25 篇上出数的东西
   （人工 gold 和 LLM 裁决在那边都不可行：没人核对过，分歧了没有裁决者）。

## 15. 本轮实测发现（详见 `eval/findings.md`）

### F-001 `pbc_21296 TABLE I` 两行压一格，`high` 且 `notes` 干净

第 41 行：`ALL-8 ALL-16` / `ALLT-cell ALLT-cell` / `> EP > EP` / `< 0.001 < 0.001` / `8.1 0.2`。
PDF 文字层里 `ALL-4`/`ALL-8`/`ALL-16` 是三个独立 word span。真值 44 个数据行，CSV 只有 43。

**三道现有防线为什么全失效**（这条最值钱）：
- 覆盖率（`COVERAGE_LOW=0.35`）：两行挤进一格，字符**一个都没丢**，只是放错位置
- pdfplumber 列数第二意见：列数是对的，错的是行
- caption 归属校验：它管的是"网格属不属于这个 caption"，另一回事

### F-002 转正判据在留出集上误报，且两条判据同时失效

`Li et al. 2025` p6：**1555 行竖排、占比 85%**，两条判据都轻松通过，但内容是
`Percentage of SBSs` / `ACA` 反复重复 —— 多面板图的 Y 轴标签。
`Williams (cells of)` p15/p16：90/44 行、67%/68%，是染色体臂轴标签。

**同时真横向表的下界被拉低了**：`Bruhm 2025` p9 是真横向表（竖排首行就是
`Table 3 | Clinically validated ctDNA tests...`），只有 **82 行 / 57%**。
而已知的多面板图上界是 79 行 / 25%（§8 的 Becker 2022）。
→ **绝对行数判据的余量从两个数量级缩到 3 行（79 vs 82）**。

试过但不成立的替代判据（都记在 `spike/probe_rotation_caption.py`）：

| 候选 | 真横向表 | 误报 | 结论 |
|---|---|---|---|
| 竖排串唯一率 | 36–89% | Li **2%** / Williams **50%** | Williams 落进真表区间 → 失手 |
| 竖排串中位长 | 1–8 | 3 | 重叠 |
| 长串(≥8字)占比 | 3–50% | 1–7% | 重叠（`pbc_29304` p4 只有 3%） |

**看起来完美的候选：竖排文字里含 `Table N` 标号**（7/7 真表有、3/3 误报无）。
**但它会误杀 `pbc_26870` p19** —— 那是真横向表，可它是**续表页、caption 为空**
（正是事实 #12 存在的那个样本）。所以修法必须是组合判据。
**这条正好印证冻结的价值：边发现边改的话，这就是一次静默回归。**

### F-005 `pbc_24724 Fig.1` 实际 `47×7`，文档记的是 `45×7`

末尾 3 行全空、表头缺首列名 `Xenograft Line` 导致整体左移、行间对齐不一致。
**两次独立运行完全一致**，所以是文档漂移不是运行波动。
`resp_30017` 的 `matched_on` 也是（文档记 `cell`，实际 `caption`）。
→ 基线表 15 条里 **2 条与实际不符，而且一直没人发现** —— 因为
`regression.sh` 原来只 `printf` 行数让人眼比对，永远 exit 0。这就是加断言器的直接理由。

## 16. eval 层的三条零标注判据与阈值

| 判据 | 阈值 | 两侧观测值 | 余量 |
|---|---|---|---|
| 区域文字层逐字比 | `TEXT_LAYER_MIN_HIT=0.90` | 文字表 98.6–100% vs 图片表 24.6–56.1% | 宽 |
| 数值列并行检测 | `NUM_COL_RATIO=0.7`、`MIN_MULTI_CELLS=2` | 正例 1 个、负例 579 行 | **单点拟合** |
| 全空行 | — | — | — |

实测：12 篇 16 张表，**只有 `pbc_21296` 被标为"没举手的错"，零误报**。

### 踩坑一：作用域必须是「页」，不能是「全文档」

`pbc_21078 fig_3` 是矢量热图，按**全文档**比对 **92.6%** 命中、看起来很干净；按**本页**只有
**56.1%**。而它的标签（`BT-29`/`KT-16`/`KT-13`/`Rh18`/`Ewing`）在 p10 的 word span 里
**一个都不存在** —— 那些是矢量轮廓不是文字。同一个词在论文别处出现（正文提到 `rhabdoid`、
`neuroblastoma`），就足以让 OCR 垃圾"验证通过"。
**顺带确认：矢量热图走 OCR 是正确的，不是缺陷。**

### 踩坑二：归一化少一条就有假报警

- **NFKC 拆连字** —— `pbc_21078` p6 文字层里是 `ﬁnal RTV`（U+FB01），docling 抽出的是
  `final RTV`。不拆连字，`Median final RTV` 在 **3 张表**上都对不上，一度以为是上标脚注问题
- **去控制字符** —— `table_ii`/`table_iii` 各有 14 个 `\x01`（符号字体的对钩类字形）
- 两条都补上之后，文字表从 "8 张 100% + 3 张有假报警" 变成 **8 张 100%、零误报**

## 17. 声明式 gold 的三条必要机制（都在真实数据上验证过）

`eval/gold.py --validate-anchors 1.output` 复跑（当时叫 gold_io.py）：

| 表 | 锚点 | 数据行 | 唯一键 | **不向下填充时** |
|---|---|---|---|---|
| `pbc_28772` | `Model+Agent` | 44 | 44 | 43 |
| `pbc_30017` | `Model+Agent` | 12 | 12 | **7** |
| `pbc_29304` | `Tumor+Group` | 41 | 41 | **23** |
| `blood` t2 | 首列`+Treatment` | 14 | 14 | **8** |
| `blood` t3 | 首列`+Treatment` | 6 | 6 | 6 |
| `pbc_26870` | `Stage+Tumor Line+Treatment Group` | 41 | 41 | 41 |
| `pbc_21078 table_i` | `Xenograft`（单列即可） | 60 | 60 | 60 |

1. **向下填充是必需的**（4/7 张表上"不填充"列明显更小 —— 合并单元格让锚点列出现空值）
2. **分节标题行要单独识别** —— `pbc_26870` 44 行里 3 行是 `Stage 1/2/3`，真数据行 41；
   `ES-4|Erlotinib` 在 Stage 1 和 Stage 3 **各出现一次**，不算 Stage 必然撞车
3. **`check_cols` 每张表不同** —— `pbc_28772` 核 1 列；`pbc_29304` 核 7 列
   （`PD`/`PD1`/`PD2`/`SD`/`PR`/`CR`/`MCR` 各占一列、格子里是**计数**）；
   `pbc_21078 table_i` 搜 `Demographic` 核 `Patient age`/`Sex`/`Stage`。
   **同为"响应表"结构完全不同，一个固定 header 套不住。**

负面测试通过：故意只用 `Model` 当 `pbc_28772` 的锚点 → 正确报出 6 组重复而不是静默按序号对齐。

## 18. 断言器（当时叫 `eval/check_regression.py`，后并入 `eval/checks.py regression`）

`regression.sh` 原来只 `printf` 行数、期望值躺在注释里，**永远 exit 0**。改成对账
`eval/expected.csv`，四类不符任一命中就 exit 1。四类都用篡改测试验证过会报：
缺失（漏表）/ 多出（误报）/ 行数不符 / 退出码不符。

**关键语义：`expected.csv` 记的是「现状快照」，不是「正确答案」。** 因为 `pbc_21296` 的正确值
是 45 行、当前是 44；按正确答案写的话断言器第一天就是红的，就不再是"防改坏"的安全带。
已知错误单独记在 `eval/known_issues.md`，下一轮统一修时一条条挪进 `expected.csv`。

**为什么人眼不够**：一张 CSV 整个消失，摘要里只是少一行；而人在核对"有的这些行数对不对"
的时候不会注意到少了一行。**人眼对「不出现」最不敏感** —— 本项目历史上三个由用户发现的
bug（§5）全是这个形状。

## 19. 留出集趟二结果（25 篇完整抽取 + 零标注判据）

**两边都跑完了**：开发集 12 篇 / 16 张表；留出集 25 篇 / 27 张表（另 1 篇正确判为 preprint 拒跑）。

| # | 问题 | 开发集 | 留出集 | 举手了吗 |
|---|---|---|---|---|
| F-001 两行压一格 | | **1/16** | **0/27** | ❌ `high`+`notes` 空 |
| F-007 无 caption 的表得 `high` | | 0/16 | **1/27** | ❌ |
| F-002 转正误报 | | 0/12 篇 | **3/25 篇** | — |
| F-005 图片表全空行 | | 2/16 | **3/27** | ✅ 都是 `low` |
| F-008 字形名泄漏 | | **0/16** | 2 篇 | 碰巧 |

**两个反差最值钱**：

1. **F-001 在留出集 0 命中 —— 但这个 0 没有判别力（我自己先下错了一次结论）。**
   我一度据此写下"它不是普遍缺陷、只在开发集出现过一次"。用户要求复查时自己查了一下：
   **留出集 27 张表里只有 2 张落在转正页上**，其中一张（`Li` p6）还是 F-002 的转正误报
   → **真横向转正表只有 1 张**（`Bruhm` p9，仅 3 个数据行）。
   留出集根本没给这类 bug 出现的机会。
   → 正确说法：**F-001 的普遍性目前无法判断。** 要判得先补横向表样本。
   **教训**：报"留出集 N/M"之前必须先问"这 M 里有几个是该 bug 可能出现的场景" ——
   分母不对，比例就是假的。
2. **F-007 / F-008 在开发集 0 命中。** 8 条 finding 里 **5 条（F-002/003/006/007/008）
   只有留出集才暴露**。§8 那个模式这一轮又重演了四次。

### F-007：无 caption 的表照样得 `high`（根因逐行核实）

`Bielski_Taylor_2021` 的期刊**目录页**（`Annual Review of Cancer Biology / Volume 5, 2021 /
Contents`）被抽成 22 行数据表，还把 p15/p16 两页**拼接**起来了。零标注判据抓到：
文字层命中率 **11.4%**（正常文字表 98.6–100%）。

根因是**两处叠加，只改一处不够**：

```python
# __main__.py:331
if caption and not rules.verify_caption_belongs(caption, dt.rows, other_captions=others):
    table.grid_status = "grid_mismatch"
```

1. `if caption and ...` —— caption 为空时归属校验**根本不被调用**，`grid_status` 保持默认 `"ok"`
2. 实测即使调用也拦不住：`verify_caption_belongs("", rows, ...)` 恒返回 `True` ——
   这是 §5 定的「**缺少证据 ≠ 有反证**」原则的必然结果。那条原则对当时那个误报场景是对的，
   但它同时让"完全没有 caption"这一档**免检**了。

**要克制的地方**：这是 `--all` 模式，按 `AGENTS.md` 它就是"跳过标号匹配、直接用版面分析找表"
的逃生舱，抽到目录页**符合语义**。问题不在抽到了，在于给了 `high`。

### F-008：docling 把字形名原样写进单元格

`/emspaceMean` / `/emspaceRange` / `/emspaceI`（`Medina`），`/bowtie`（`Chang`）。
全语料扫描：`/emspace` 27 次、`/bowtie` 1 次，**开发集 12 篇零出现**。

## 20. 判据本身被留出集纠正了两次

**这一轮我的检测器误报了两次，都是留出集补上的中间档**（跟 §8 完全同构，只是这次错的是 eval
不是产品）：

| 误报 | 原因 | 修法 | 修后 |
|---|---|---|---|
| `Bao 2025` 两张表（94.1% / 85.7%） | docling 把**多级表头**用 `.` 拼平成 `Training.Cancer.n =2,254`，整串不在文字层里 | 表头行豁免：按 `.` 拆开、每段都在就算命中 | 两张回到 100% |
| `Bruhm 2025` 横向表 Table 3（83.9%，**跌破硬阈值**） | **单层**表头也被加尾点（`Sponsor.` / `Cancertype.`），豁免要求拆出 ≥2 段所以不生效 | 放宽到 ≥1 段 | 回到 100% |

两次修完都复验了 `pbc_21296` 那个真 bug **仍然被抓着** —— 豁免只对表头行生效，
数据行不动（真 bug 在数据行，`< 0.001 < 0.001` 按 `.` 拆开每段也会碰巧命中，
给数据行豁免就等于把判据关掉）。

**阈值真实余量（第二处自查修正）**：`TEXT_LAYER_MIN_HIT=0.90` 早先记的"两侧 98.6% vs 56.1%、
中间空 42 个百分点"是**错的** —— 那是"文字表 vs 图片表"的间隔，而阈值实际要分的是
"好文字表 vs 坏文字表"。全语料 30 张文字表排序后：**74.3% ← 90% → 95.5%，中间 21.2 个百分点**，
且**负例只有 2 个数据点**。按本项目惯例应归入"余量偏紧"一档，不是"宽"。

**判据的噪声底**（`text_layer_partial` 软信号，命中率仍 ≥90%，**不要当缺陷看**）：
多级表头残留（`Validation cohort.Specificity...`）、符号字形不一致
（`Lipofectamine Ò 3000` 对文字层里的 `\x02`）。只有跌破 90% 硬阈值的才值得追 ——
全语料 43 张表里只有 3 张。

## 21. F-003：用非循环判据把"疑似漏检"从 86 处压到 8 处

留出集 25 篇只检出 11 张表（平均 0.4/篇），17 篇是 0 张。直接问"caption 判据找到几个"是
循环论证，换个角度：现行判据要求标号在**行首**，那就对比「行首规则找到的」与「全文任意位置
出现的 `Table N`」，**差集就是"标号出现了但不在行首"**，逐条看上下文即可分辨。

结果 8 处**全是引用**（`Supplementary Tables S7-S8)`、`Table S1A)`、
正则误切的 `Supplementary Table 952)`、`Chang` 的 Key Resources supplementary 表）。
另有 4 篇全文一次都没提到表。→ **倾向"真没有"**，人工抽查从"必须"降级为"可选"。

**踩坑**：探针第一版报 86 处，全是假的 —— `REFERENCE_CUE` 把 `\)` `\.` 和单词写进同一个
`(?:...)\b`，而**标点后面跟空格构不成词边界**，所有括号引用一个都没被识别。
**`\b` 不能收在标点后面。**

## 22. 本轮没做的（交接给下一轮）

- **LLM 第二读者未实跑** —— 代码就绪（`eval/llm.py`，当时叫 llm_review.py，`claude-opus-4-8` + Batch API +
  structured outputs，喂整页原图），`ANTHROPIC_API_KEY` 未设置。它负责的两类错本轮
  **完全没有覆盖**：**错误断列**（用户点名，便宜启发式实测 2/2 全误报）与**图片表字形错**。
- **接缝行数校验未实现** —— 需先 spike，见 `NEXT.md` §3。
- **`eval/score.py` 刻意没建** —— 后三个数都要 gold，而 gold 要人裁决 LLM 分歧才存在。

## 23. 修掉 5 条已验证的阈值（2026-07-26 晚）

冻结解除，只改**已离线验证过**的五条。**每改一条跑一次 `eval/regression.sh`，全程零回归。**

| # | 改动 | 验证 |
|---|---|---|
| **F-011** | 扫描件判据从「单页字数」改成「整篇整页位图占比 >=80% 且 平均 >=1000 字/页」 | 21 真 / 5 假完美分离、余量 **76pt**；开发集仍是 EAP 3/3 + rstl 5/5 那 8 页 |
| **F-010** | `is_preprint` 加第二条：第 1 页左缘竖排的「平台名+编号」 | 90 篇零误伤、4 篇 arXiv 全中；检出 **1/4 → 4/4** |
| **F-008** | `rules.strip_glyph_names` 清空白字形名 | 24 格内容全保住、0 清空；开发集 0 格受影响 |
| **F-009** | 标号正则加 `Extended Data`/`Supplemental`/`Supplementary Data`/`Online`；`normalize_label` 折 `supplemental`→`supp` | 开发集 caption 集合**零变化**；留出集 +60 真 caption |
| **F-002** | 转正加第三条「竖排含 `Table` 标号 或 上一页已转正」 | 三语料 **9 真表全中、0 误报、0 漏判**（改前留出 2 + 验证集 13 个误报） |

**单测从 65 涨到 71**，每条改动都配了带实测依据的用例。

### 三处实现上的坑

1. **F-011 要改成两趟。** 「是不是扫描件」是**整篇**属性，而 `read_doc` 是逐页构造 `PageInfo` 的。
   改成：第一趟全部 `ocr_text_layer=False`，读完再算整篇比例、回填。
2. **F-002 要改函数签名。** `rotation_for_page(vert_lines, dominant_dir, horiz_lines)`
   **拿不到文字内容**，加不了「竖排含 Table 标号」这条。改成 `_line_dirs` 顺带返回该标志、
   `read_doc` 再传上一页的转正状态。新参数设成 keyword-only 有默认值，但默认 `False`
   会让老调用返回 0 —— **4 个旧单测因此失败，是预期的，逐个更新了**。
3. **F-010 差点漏接线。** 加了 `DocInfo.p1_watermark` 字段、也在循环里算了，
   但构造 `DocInfo(...)` 时忘了传 —— 那字段会永远是空串、判据永远不生效。
   **加字段和传字段是两件事。**

### 一个副产品：`holdout/report.py` 自动转绿

F-002 修好后，留出集体检从「▲ 1 项已知失准」变成「✓ 全部通过」，
`KNOWN_FAILING` 里的 `rot` 条目随之删掉。**这正是当初设计三档报告（`✓ / ▲已知 / ✗新失准`）
时写的验收标准 —— 修好后本项应转绿。** 它兑现了。

### 明确没做的

- **F-005**（图片表全空行）：修了会**有意改基线**（`resp_24724/p05_fig_1` 47→39 行），
  不属于"零回归"这一批，要单独走改基线流程。且删完空行仍差 6 行（人眼核对值是 45）。
- **F-001**（两行压一格）：根因未定位。要先拿未转正版本跑对照，判定是 docling 行切分错
  还是转正坐标问题。归到 eval 之后的矫正阶段（用户 2026-07-26 定）。
- **F-007**（无 caption 得 `high`）：**撤回**。规则太粗会冤枉 Cell Press 的 Key Resources Table
  （`Chang p15` 95.9%、`Zhu p18` 100% 文字层命中，是真表）。等文字层判据进产品后用它降级更准。
- `eTable`（JAMA）/ `Additional file`（BMC）：前者 0 实例无从验证；后者工具已靠内嵌的
  `Table SN` 正确处理成"只被引用"，加了反而让同一实体出现两个标号。

## 24. F-013（legend 续段）：查清了，然后搁置

**这条是"跑完了但没读"捡回来的。** 余量探针扩到三语料后我只看了 OCR 那一节，
legend 的结果一直躺在日志里 —— 用户问"是不是都收敛了"才逼我去读。
**教训：跑了不等于看了。**

### 实测：比"余量最紧"还糟

`log.md` §11 记的是「107 vs 36，只差 4」。90 篇实测：
**停住的最高 39 ← 阈值 40 → 吸收的最低 40，两侧余量都是 0。**

而且边界上**两类错同时存在**（26 个紧贴阈值的案例）：
- 该吸收却停住 5 处 —— `genotoxic stress or oncogenic signals.`（38 字，明显是 legend）
- 不该吸收却吸了 8 处 —— `Number of patients (n)`、`Train`、`Test ID`（表头），
  目录的点引导线，以及**期刊页眉**：`384 | Nature | Vol 618 | 8 June 2023`（`Karlsson`，留出集）、
  `Cancer Cell 43, 1125–1140, June 9, 2025`（`Zhu`，留出集）

**根因跟 F-011 同型**：表头能 41 字、legend 能 38 字，**长度这个维度没有信息量**。

### 找到了修法方向，但没做

排除了三个候选（句末标点被尾部页码破坏、平均行长/行数重叠、点引导线只抓目录），
**虚词密度**看起来对：真 legend 17–57%，坏例 **11/12 精确为 0%**。
对比现行判据 12 个坏例放过 8 个。

### 用户决定搁置 —— 理由要写清是哪一类

**是「影响面不够大」，不是「选择性不够」，也不是「技术做不到」。**
它只影响 legend 文本 → 只影响"靠 caption/legend 命中关键词"这条路，**不碰表格内容**。
相比 F-001（表少一行还标着 `high`）优先级低得多。

**恢复工作时别从头来**：修法方向、三个已排除的候选、24 个手工标注的边界样本、
以及"不要为那 1 个漏网再加条件（那是单点拟合）"这个判断，全部记在
`eval/findings.md` F-013 里。探针 `spike/probe_legend_criteria.py` 改个判据就能复跑。
第一步应该是在**全部 507 个决策点**上跑，而不是拿这 24 个边界点定阈值。

---

## 25. 坐标判据：补上「位置」这个从来没验过的维度（2026-07-26 夜）

### 25.1 起因：内容判据已经饱和，而它对位置错误天然全盲

`eval/checks.py` 的文字层判据只问「格子里的字串在该页文字层里存在吗」。
实测**开发集 12 张文字表全是 100.0% 命中、零告警** —— 判据在这批料上已经没有区分力了，
它证明不了那 12 张是对的，只能证明字都是真的。

而 F-001（`pbc_21296` p4）把两个数据行压进一格，**字符一个都没丢**，所以覆盖率、
文字层命中、pdfplumber 列数第二意见三道防线全过，最终 `confidence=high`、`notes` 空。

→ **所有内容类判据对位置错误免疫。这不是阈值调得不好，是维度不对。**

被压掉的那行正好是 `ALL-8` + `ALL-16` 两个瘤系连同各自的 response
（`Int High` / `High High`）—— 直接毁掉用户要的「PDX × 处理 × 响应」三元组。
这条把 F-001 的危害从"抽象的抄错"落实成了"目标数据被毁"。

### 25.2 推翻了 NEXT.md 的一个判断：「7 条阈值从没被碰到」

`NEXT.md` §1 说有 7 条阈值"完全没碰到"，理由是 `--list` 不走抽取路径。
**这个推理把"没被量过"说成了"没被执行过"** —— `1.output/` 里明明有 16 张抽出来的表，
那些阈值天天在跑，只是没人量过它两侧的余量。两件事的应对完全不同。

### 25.3 两个检测器，成本差一个数量级，结论一致

**D-cell（便宜）**：`ti.data.table_cells` 每个 cell 自带 bbox、`start_row_offset_idx`、
`row_span`。压行的 cell 会明显比同列其他 cell 高。

| pbc_21296 p4 首列 | 高度 |
|---|---|
| row 40 `ALL-7` | 8.77 |
| **row 41 `ALL-8 ALL-16`** | **15.78（1.85x）** |
| row 42 `ALL-17` | 8.65 |

判据：**排除表头行后，同一数据行有 >=2 列的 cell 高于该列中位数 1.5x**。
实测 **28 个页-表命中 1 个**，就是 `pbc_21296` p4 行 41（**7 个列同时超标，1.83–1.91x**），
**零误报**。

**表头行必须排除**：`pbc_21296` p4 的表头合法折行（`KM estimate of median time to event a`），
6 个列全部超标 2.07–2.24x。不排除就是 100% 误报。

**这份数据 docling 一直在算，代码从来没读过** —— `engine_docling._df_to_rows`（L53-61）
只用 `export_to_dataframe()`，那个 API 只返回字符串，几何信息全丢。

**D-band（通用）**：把词按行轴聚类成带，跟 docling 行数比。M1（中心+gap）实测
**一致 20 张 / 带多 2 张 / 带少 6 张**；M2（区间重叠合并）**一致 13 / 带多 1 / 带少 14**
—— **M1 明显更好**，M2 在 `pbc_30017` p7 上把 12 行合并成 5 个带。

**「带多」才是 F-001 那类错**（docling 丢了行）。排除工具本来就拒跑的 preprint `566455`
之后，**开发集 19 张表里带多方向 1/1 真阳性、0 误报**。

### 25.4 F-001 根因判定：H1，`export_to_dataframe` 是无辜的

`ALL-8 ALL-16` 在 `table_cells` 里就是**一个 cell**（`row=[41,42)`、`row_span=1`），
不是"两个 cell 被 dataframe 压平"。
→ **是 docling 的表结构模型（TableFormer）自己合并的，修法不在 `_df_to_rows`。**
候选方向：换 `TableFormerMode.ACCURATE`（`AGENTS.md` 记它可用，当前代码没设），
或用 cell bbox 高度 + 行带做后处理拆分。**都未验证。**

### 25.5 踩的四个坑（连同当时为什么那样想）

1. **以为归一化 PDF 会把坐标一起转正 —— 不会。**
   实测 `page.rotation` 报 90、`page.rect` 确实换成 792x594，**但 `get_text` 返回的坐标
   一个字都没变**（`pbc_21296` p4 首词 `Pediatr` 两边 bbox 完全相同，445 个竖排行照旧）。
   而 **docling 用的是旋转后的帧**（它报页宽 792，表 bbox 的 x 到 730.6，超过未旋转页宽 594）。
   两者不在同一坐标系，不做 `page.rotation_matrix` 变换就拿 bbox 去框词，**一个词都框不到**。
   当时以为"设了 /Rotate 就万事大吉"，因为 `pdfio.write_normalized` 的注释说
   docling 结果与逐页转正逐字一致 —— 那说的是 docling，不是 PyMuPDF 的取词。

2. **拿整页判文字方向 —— 续表页会被误判成「方向混杂」跳过。**
   `pbc_28772` p4（76%）与 `pbc_21078` p5（71%）都栽在这，而表体本身方向一致。
   **必须先按 bbox 过滤再判方向。**

3. **表头区不能只跳过第一个带。** 多行表头会切成 2–3 个带，只跳一个就把剩下的表头行
   当成数据行 —— `pbc_28772` p3、`blood` 那三张的「带多 1~2」全是这么来的。
   改成用「第 1 个数据行 cell 顶边的**中位数**」划界后，四张全部转 ✓。
   中位数不能换成 min：纵排标签格（`566455` p61 的 `Inferred Ethnicity`）bbox 顶边会
   高过第二行表头。

4. **`python -c` 里 f-string 不能含反斜杠转义的引号** —— 反复踩，浪费了三四次调用。
   要跑多行分析就老老实实写文件。

### 25.6 阈值：诚实分档

`GAP_M1 = 3.0`（M1 的带间隙）。28 个页-表实测：
**带内最大 gap = 1.50pt ← 阈值 3.0 → 带间最小 gap = 3.61pt**。
下侧 2.0x、**上侧只有 1.2x（0.61pt）**。带间最小的三张是 3.61 / 7.02 / 7.94 ——
**3.61 那张（`pbc_30017` p7）是孤点，离第二名有一倍差距。上侧余量偏紧，建议后续改自适应。**

`CELL_TALL = 1.5`（D-cell 的高度倍数）：正例 1.83–1.91x、表头折行 2.07–2.24x（已排除）、
其余全部 < 1.5x。**负例侧没有贴着阈值的样本，所以余量看着宽但不算量过。**

### 25.7 未消掉的一个假阳性（**停手，不再调**）

`566455` p61 第二张表：docling 5 行、M1 报 6 行。成因已诊断 —— 两级表头
（docling 把 `Reported Ethnicity` 与子标签拼成 `Reported Ethnicity.African`，即 PDF 事实 #16），
物理页面上表头占两行，而 docling row-1 cell 的 bbox 越过了第二行表头。
**试了三种划界法都没消掉，就此停手** —— 再调就是拿单个样本拟合，正是 F-013 记过的坑。
影响可控：`566455` 是 preprint 负例，工具拒跑（退出码 3），它的表根本不会被产出。

### 25.8 顺带查出的基线偏差（**没人发现过**）

拿 `AGENTS.md` 人核值比 7 张表，6 张吻合，唯独
**`pbc_21078 p04_table_i` 实际 60 个数据行，AGENTS 记 `43+16=59`，多 1 行**。
`gold.py --validate-anchors` 显示这 60 个锚点**全部唯一**，所以不是重复行。
它也是横向+跨页表，与 F-001 同类。**待查，先记在这。**

另外核实：`eval/expected.csv` 21 条表条目里 20 条能与 `1.output` 交叉核对、全部一致
（1 条 EAP 无对应产物）。所以它**自洽**，但 `known_issues.md` 明说其中 2 条故意钉着错值。
→ **`expected.csv` 只能当「行为没变」的快照，不能当「正确答案」。**

### 25.9 修掉的两个 eval 自身 bug

- `eval/report.py:73` 取 `sys.argv[1]`，而 docstring 与 `eval/matrix.sh:28` 都按
  `report.py matrix 1.output` 调用 → `Path("matrix").iterdir()` 抛 FileNotFoundError，
  **照文档跑必崩**。改成取最后一个非选项参数。
- `eval/llm.py:126` 只注册 `p.stem`，`EAP in advanced gastric cancer..pdf` 会被静默跳过
  （输出目录是 `sanitize_name` 的产物）。改成双键注册，照 `eval/checks.py:433-438`。
  **只能靠单测钉住** —— 开发集里 EAP 那篇一张表 CSV 都没有，跑流水线看不出差异。

### 25.10 产品代码零改动（已验证）

本项目没有 git，所以用校验和当 diff：开工前存 `eval/_baseline/product_md5.txt`（10 个文件），
收工比对**全部一致**。`pytest` 71 → 72（新增那条单测），`bash eval/regression.sh` 快档全绿。

### 25.11 打分器（`eval/gold.py --score`），**刻意写在 gold 数据之前**

顺序是硬要求：**先有打分器、后有 gold**，否则会不自觉地照着答案拟合。

补了 `gold.py` 的五处缺口（原先 `load`/`validate_against` 是死代码、无调用方无单测无数据）：

1. **结构化结果 + per-column 分解** —— 原来只返回 `list[str]`，没法算准确率
2. **`source_filter`** —— `gold.sources` 早就读进来了但从没被用过。默认只算 `human`；
   跑一次 `human,agreed` 的差值就是「LLM 与工具一致但没人看过」那部分的规模
3. **空值三态** —— 原来 `if wv and ...` 把「期望这格为空」和「还没标」混成一种且都静默跳过，
   **会让漏抽白得分**。改成 `<EMPTY>` 哨兵 / 留空=unlabeled 不进分母 / 有值正常比。
   **哨兵不能用 `-`** —— `pbc_28772` 里 `-` 是真实值（"不适用"）
4. **`#N` 列序号** —— 图片表的表头本身就是 OCR 垃圾（`pbc_21296 fig_1` 是
   `Linte / Histology / Score / Difference mruTTe / Growp Response`），按名字根本引用不了。
   不加这条，5 张 gold 里那 2 张图片表写不出来
5. **锚点不唯一不再 raise** —— `pbc_24724 fig_1` 末尾 3 行全空 → 锚点全是空串 → 必崩，
   而那恰恰是最需要打分的一张。改成记 finding、继续出聚合数

**非对称契约**：`CellVerdict` 只有 `ours` 字段，**代码层面就不存 gold 的期望值**。
`tests/test_eval.py::TestScorerDoesNotLeakGold` 用 `ZZZSECRET` 钉死这一点 ——
**那条单测才是隔离的真正保证**，`.claude/settings.local.json` 的 deny 规则挡不住 `open()`。

**踩坑**：`_parse_header` 把 `#` 一律当注释起始，`check_cols: #1` 被整行吃空；
改成 `\s#` 之后又被 `#` 前那个空格命中，值仍是空。两次都报"头部缺字段 ['check_cols']"，
跟真因隔着一层。最终判据是 **`#` 后面跟数字就不算注释**。

单测 71 → 78。收尾复核：产品代码校验和零改动、`detect` 与基线逐字节一致、
`gold --validate-anchors` 仍 7/7、快档回归全绿。

### 25.12 删掉 9 个一次性探针

`spike/` 16 个文件里只有 4 个被文档引用。删掉 9 个（782 行）：`probe_docling` /
`probe_text` / `probe_image` / `probe_rotate` / `probe_upscale` / `probe_new3` /
`probe_prereq` / `probe_gate_timing` / `probe_plumber_in_bbox` ——
结论早已迁进 `AGENTS.md` 的 PDF 事实和各 engine 的 docstring，探针本身是一次性的。
备份在 `/tmp/pte_spike_backup/`（本项目没有 git，删了不可逆）。

保留 7 个可复跑的：`probe_candidate_fixes`（新探针的范式）、`probe_threshold_margins`、
`probe_legend_criteria`、`probe_rotation_caption`、`probe_zero_table_papers`、
`build_threshold_corpus` + `probe_find_testmaterial`（成对）。

### 25.13 dry-run 抓到一个真问题：横向表被躺着喂给 LLM

给 `eval/llm.py submit` 加了 `--only`（原先会把 16 张全提交）和 `--dry-run`
（渲染 PNG + 落 prompt，**不调 API**）。第一次 dry-run 就值回票价：

**`render_pages` 渲染的是原始 PDF，于是横向表整张躺倒** —— `pbc_21296` p4 出来是
1650x2200 的竖图，表要从下往上读。模型能读旋转文字，但这是白白加难度，
而 gold 初稿的质量决定后面所有打分的可信度。

**根因是 `get_pixmap` 与 `get_text` 行为相反**：
- `get_text` 在设了 `/Rotate` 的页上返回**未旋转**帧（§25.5 坑 1）
- `get_pixmap` **会**套用 `/Rotate`

所以只要改喂归一化 PDF 就正过来了：`pbc_21296` p4 → 2200x1650、
`pbc_26870` p18+19 → 2200x3400（两张横向页竖向叠）。

**人眼复核 `pbc_26870` 那张拼图**（横向 + 跨页 + 续表 caption 为空，最难的一张）：
页序对、两页同尺度、都正立。并且**顺带核实了 gold 的期望值** ——
p18 有 24 个数据行 + 2 个分节行 = 26，p19 有 14 + 1 = 18，与 `AGENTS.md` 的
`26x8 + 18x8` 完全吻合；合并后 **38 个真数据行 + 3 个分节行 = 41**，
正是 `gold.py --validate-anchors` 早先算出的 41 个锚点。

**另外用人眼在 `pbc_21296` p4 的渲染图上数了首列**：
`BT-29 … ALL-7, ALL-8, ALL-16, ALL-17, ALL-19` 共 **44 个瘤系**，
与坐标法的 44 一致、与 docling 的 43 不一致。且 `ALL-8` 的 `Median final RTV` 是 `8.1`、
`ALL-16` 是 `0.2` —— 正是被挤进一格的那个 `8.1 0.2`。**F-001 至此三方证据齐了。**

`.claude/settings.local.json` 已加 gold 目录的 deny 规则（15 条，覆盖 Read 与
cat/head/tail/less/more/open/grep/rg/awk/sed）。**再说一遍：deny 只挡顺手读，挡不住
`open()`。真正的保障是打分器的非对称契约 + `ZZZSECRET` 单测。**

### 25.14 LLM 第二读者实跑：F-001 的第四个独立证据

`eval/llm.py` 加了 `--only` / `--dry-run` / `gold-draft` 三样，在 5 张 gold 表上实跑了一轮
Batch（`msgbatch_01H36vBfVDDUGFAaqVXiEmdQ`，5/5 成功）。

| 表 | LLM 读到 | 工具 | 待裁决 |
|---|---|---|---|
| `pbc_21296 p04_table_i` | **44** | **43** | **2** |
| `pbc_28772 p03_table_1`（负例） | 44 | 44 | 6 |
| `pbc_26870 p18_table_1` | 41 | 44（含 3 分节行 = 41 真数据行） | 1 |
| `pbc_24724 p05_fig_1` | 41 | 46 | 41（全部） |
| `pbc_21296 p03_fig_1` | 45 | 44 | 41 |

**F-001 至此四方独立证据**：docling 自己的 cell bbox（1.85x 高）、词坐标聚类（44 带）、
人眼数渲染图（44 个瘤系）、LLM 读页面图（44 行）。工具的 43 是错的，没有疑问了。

**负例表现良好**：`pbc_28772` 两边都是 44 行，只有 6 行有分歧（待人裁决，多半是 LLM 读错）。

两张图片表全部待裁决，**这是正确信号不是 bug**：`pbc_24724 fig_1` 工具侧 `#0` 列是
`Histology`（首列名缺失导致整体左移，就是 F-005），锚点按构造对不上；
`pbc_21296 fig_1`(120dpi) 的瘤系名被 OCR 读成 `K1-18`/`Rhabdold`，锚点匹配失败是预期内的。
两张本来就要人通读全表（`NEXT.md` 里 L3 那一档）。

**踩坑：分节行让 `pbc_26870` 41 行全部假分歧。** prompt 明确要求 LLM 不要输出
`Stage 1` 这类分节标题行，所以 LLM 侧锚点是 `|ES-2|Patritumab`、工具侧是
`Stage 1|ES-2|Patritumab`，**永远对不上**。修法：LLM 侧一律按 `section_rows=False` 算锚点，
再**按行序**把工具侧的分节标签搬过去 —— 且**只在行数相等时搬**（行数不等时位置对齐会把
「漏 1 行」放大成「错 40 处」，正是 `gold.py` 反复警告的那件事）。修后 41 → **1** 行待裁决。

---

## 26. 逐个方法量准确性（2026-07-27）—— 第 1 步：docling

### 26.1 起因：上一轮只量了一个方法，而工具有三条路

`log.md` §25 做完了坐标判据、打分器和 5 张 gold，**但只量了 docling**。
用户指出这是设计缺陷：目标是"验证 `pdf_table_extract` 的准确性"，而工具里有
**三条抽取路径** —— docling（文字表）、pdfplumber（现在只贡献一个"列数第二意见"标量）、
paddle OCR（图片表）。只量一条，回答不了"准确性如何"。

**我第一次尝试对比时犯了个更糟的错**：把三个方法混在一张表里输出，
结果 plumber 那列全是 `—`、OCR 两张全是 `0`。那不是"分低"，是**我没给它们能对齐的打分方式** ——
**等于没测，却看着像测过了**。用户当场指出，并要求**一次只测一个方法**。

→ 拆成四步：docling → pdfplumber → paddle → 汇总。每步独立探针、独立报告、独立验收。

### 26.2 docling 的分

文字表 **164 格、命中 163 = 99.4%**（`human+agreed` 档）。
逐张见 `eval/findings.md` 的「第 1 步」一节。

**两个报告纪律，都是这一步立的**：

1. **「无法对齐」≠「0 分」。** 上一版把图片表写成 `0`，会被读成"OCR 全错"。
   实际是锚点配不上、根本没法逐格比。两者必须用不同记号。
2. **每一处漏行都要能归因**，否则报告退化成"有问题"三个字。

### 26.3 归因规则跑通了（不靠人工贴标签）

拿 gold 锚点与实际锚点的**差集**比对，规则判：

| 观察 | 归因 |
|---|---|
| 多出行去掉 `- ` 后等于漏行 | **F-017** 断行连字符 |
| 多出行去掉尾部孤立标记后等于漏行 | **F-016** 脚注上标 |
| 两个漏行被同一个多出行包含 | **F-001** 两行压成一格 |

实测：5 处 `EPZ011989 + cyclophosphamide` 全部自动归到 F-017、
2 处 `ALL-8`/`ALL-16` 归到 F-001，**零「原因不明」**。
探针在没归上的时候会 exit 1 —— 让"查不出原因"变成硬失败，而不是一句含糊的备注。

### 26.4 ⚠ 一个会被读反的数字：`human` 档不能当准确率

`human` 档每张表只有 1–2 个计分格，因为**用户只裁决了有分歧的行**，
而有分歧的行**恰恰是工具出错的行** → `human` 档命中率**天然向下偏**
（实测 `pbc_28772` 是 0/1 = 0%，看着吓人，其实只是那一格确实错了、样本量为 1）。

反过来 `agreed` 档是"LLM 与工具一致、没人看过" → **天然向上偏**。

**两档都不能单独当准确率读。** 真值在两者之间。要收紧只有一条路：
让人**通读一整张表**（不只看分歧行），量"一致 = 正确"的成立率。

### 26.5 第 1 步验收

4 条专属 + 3 条共通全过：图片表标了「无法对齐（原因）」而非 0、零「原因不明」漏行、
两档都出且给了解释、结论进了两份文档；产品代码 md5 无差异、`pytest` 78 passed、
`eval/regression.sh` 快档全绿。

`spike/probe_engine_compare.py`（那个混了两个引擎的）已删，换成 `probe_engine_docling.py`。

## 27. 第 2 步：pdfplumber —— 项目里第一次逐格量它（2026-07-27）

### 27.1 它一直被低估，而且低估的理由是个误会

生产里 plumber 只贡献"列数第二意见"这一个标量：`extract_in_bbox` **返回的是完整二维网格**，
却被 `columns_in_bbox` 用 `max(len(r))` 降成 int 扔掉。`log.md` 此前**没有任何逐格实测**。

`pbc_28772` p3 它抽出 83 行，一直被当作"对不齐"的证据。实际是
**38 个数据行中间夹着空行**，内容与 docling **逐字相同**。去掉空行就是 45 行。

### 27.2 关键实测：**数据行优秀，表头不可用**，而根因不是我先猜的那个

`pbc_21296` p4 的数据行与 docling 逐字相同，但表头是 `'M FST/Cc fina'` 这种乱码，
分节标签 `Stage 1` 被抽成 `tage 1`、`Tumor Line` 抽成 `mor Line`。

**我第一个猜想是 docling 的 bbox 左边缘切进去了**（`engine_paddle` 有 `CROP_PAD_RATIO`
就是干这个的），于是量了 pad=0/1/2/3/5/8 —— **六档结果完全一样**，猜想被证伪。

真正的原因：**plumber 的列边界是从数据行的对齐推出来的，而表头与分节标签是居中排版的**，
字符按 x 落到边界外就被丢掉。数据行左对齐，所以完好。
→ 表头一律借用 docling 的；分节表直接标"测不出"，**不做模糊修复**
（拿 gold 的知识去补引擎输出，等于把"测不出"粉饰成"测出来了"）。

### 27.3 新增「忽略空格」档 —— 不分档会得出错误印象

`pbc_21296` plumber 严格 **84.1%**、忽略空格 **100.0%** ——
**14 处严格失配全部是纯空格问题**（`ALVrhabdomyosarcoma`）。
只报 84.1% 会把"plumber 不行"这个错误结论钉死。
（但丢空格对目标 (b) **仍然有害** —— 匹配不上受控词表。只是修法完全不同。）

`gold.score_against` 加了 `ignore_spaces` 参数，两档一起报，差值就是纯空格问题的规模。

### 27.4 **两个引擎互补，这次有完整数据了**

`pbc_21296 p04` 跨引擎逐格对比（**全部列，不只 check_cols**）：
共同行 42/43，格子**相同 327 / 仅空格差 91 / 真分歧 2**。

| | docling | plumber |
|---|---|---|
| `ALL-7` 的 `EFS T/C` | `> 6.6` ✅ | `>6.6 1` ❌ |
| `ALL-7` 的 `Median final RTV` | `10.8` ✅ | `0.8` ❌ |
| `ALL-8` / `ALL-16` | ❌ 压成一格 | ✅ **两行都对** |

→ **plumber 的切列错在这张表上共 1 处（波及 2 格）；docling 的压行错 1 处（丢 2 行）。**

**必须看全列才能发现切列错** —— 它落在 `Median final RTV` 上，而那**不是 check_col**。
只看打分列会漏掉整整一类错误。这条要记住：**gold 的 check_cols 是给"目标 (b) 准确率"用的，
不能拿它当"引擎质量"的全部**。

### 27.5 三张表上谁更好（**样本只有 3 张，不能外推**）

| 表的性质 | 更好 | 为什么 |
|---|---|---|
| 横向表、列宽均匀、无跨行单元格 | **plumber** | 44 行全对（docling 43），忽略空格 100% |
| 列多有窄列、有折行单元格 | **docling** | plumber 合并了两列（10 vs 11）→ 从第 6 列起系统性错位；还把折行单元格拆成两行 |
| 有分节标题行 | **docling** | plumber 分节标签被截断 |

**注意 `pbc_28772` 的 114 处「真分歧」不是 114 个独立错误** —— 是 1 处列合并造成的系统性错位。
报"分歧格数"时必须说清这一点，否则会严重夸大。

### 27.6 第 2 步验收

5 条专属 + 3 条共通全过。产品代码 md5 无差异、`pytest` 78 passed、快档回归全绿。

## 28. 第 3 步：paddle OCR —— 图片路径的第一次真实测量（2026-07-27）

### 28.1 解开 3x/4x 矛盾：**文档没错，但也没说全**

`AGENTS.md` 事实 9d 记「裁剪 + 4x → `45x6`」，而 `upscale_factor(120)` 实际返回 **3**。
用 `spike/lowres/` 现成的 8 张对照图（已删的 `probe_upscale.py` 留下的）跑完：

- **裁剪必须**：1x/2x/3x/4x **不裁剪一律 0 表**，无一例外
- **2x/3x/4x 形状完全相同**（都是 `45x6`），1x 只有 `44x5`
  → 文档记的 4x **不是唯一解**，生产的 3x 没问题，**矛盾解除**
- **但文字质量随倍数上升**：表头 4x=`inte | Histology | Score`、3x=`ie | HISOgY | Score`、
  2x=`TAD | | Core`。→ 提到 4x 能在**零形状代价**下改善识字（是否普适未验证）

`CCR p5` 八个变体全失败 —— 但按事实 9b **那是正确行为**（western blot 面板，不是表）。

### 28.2 两张图片表都落到 L3，**但 L3 给出了第一个站得住的数字**

| gold | 降级原因 | 响应码多重集重叠 |
|---|---|---|
| `pbc_21296 fig_1`(120dpi) | L1 锚点重复 1 组；L2 配对率 27% | **39/44 = 89%** |
| `pbc_24724 fig_1`(300dpi) | L1 锚点重复 8 组；L2 配对率 **0%** | **34/41 = 83%** |

**L2 失败不是阈值定严了**，是 OCR 把瘤系名毁得太彻底（`BT-29` → `-29`，首字母整个没了）。
模糊配对要求"最优唯一"，`KT-13`/`KT-16` 距离相同时**宁可拒配也不瞎猜** —— 配错比不配更坏。

**「不管位置、抽对了几个响应码」= 83%–89%**，这是图片路径目前唯一站得住的准确性指标，
而且它正对着目标 (b)。

### 28.3 `pbc_24724` 的左移**逐行不一致** —— 排除了最便宜的修法

用「列内容类型一致性」量（不依赖 gold）：46 行里**只有 27 行完全符合各列主类型，
19 行有 1–4 列偏离**。→ **不是整体左移一位，补一列修不好。**

这条的价值在于**排除**：F-005 的修法假设里"整体左移 ⇒ 补一列即可"被证伪了。

`pbc_21296` 则是另一种病：第 0 列 **17/44 行被读成数字**（丢首字母），
而列 1（`Histology`）44/44 全对。**同为"图片表"，两张的失效模式完全不同。**

### 28.4 🐛 差点整步都是假数据

第一次跑出来「响应码 gold 0 个」，看着像"gold 里没标"。实际是**打分器读取时就丢了**：
图片表的 `check_cols` 是 `#4`/`#5`，而 gold 正文列名是 LLM 读到的真实表头
（`gold-draft` 按用户要求保留了全部列），`row.get("#4")` 全返回 None。

**这个 bug 的形态值得记**：它不报错、不崩溃，只是让分母变成 0，
而 0 分母在报告里长得像"这项没数据"，**跟"读取失败"看不出区别**。
修法：`load()` 把 `#N` 解析成正文第 N 个数据列。**没有重新生成 gold** ——
用户已经人工订正过，重生成会毁掉那份工作。

### 28.5 顺带：产物与 fixture 对不上

变体矩阵 `裁剪+3x` 出 `45x6`，生产产物 `p03_fig_1.csv` 是 `44x5`。
`spike/lowres/` 的图裁剪框可能与生产不同。**未查清**，记下来 ——
这是"文档实验结果"与"生产实际产出"之间的第二处缺口（第一处是 3x/4x）。

### 28.6 第 3 步验收

6 条专属 + 3 条共通全过。产品代码 md5 无差异、`pytest` 78 passed、快档回归全绿、
gold 锚点自检 7/7。

## 29. 第 4 步：三方汇总 —— 逐个方法量完之后（2026-07-27）

完整的「场景 × 引擎」表在 `eval/findings.md` 第 4 步一节，每格带证据指针。这里只记判断。

### 29.1 一句话结论

**三个引擎的失效模式完全不重叠，所以谁也替代不了谁：**

| 引擎 | 错在哪一层 | 根因 |
|---|---|---|
| docling | **行**（F-001 两行压一格） | 行由 TableFormer **模型**判定，模型会错且无信号 |
| pdfplumber | **表头/分节/空格**，行反而不会错 | 列边界从**数据行对齐**推出，居中排版的字符落到边界外被丢 |
| paddle OCR | **字**（`BT-29`→`-29`）+ 逐行不一致的列错位 | 输入是像素，无本地参照 |

**docling 错在行、plumber 错在头、OCR 错在字。** 这三句话是本轮四步的全部收敛结果。

### 29.2 最有操作价值的一条：**行数第二意见现在能做了**

`rules.audit`（`rules.py:532`）现在只比**列数**，而 **F-001 恰恰是"列数对、行数错"** ——
这就是为什么三道防线全过。而 plumber 去掉空行后的行数在 3 张表上是 44/26/42（真值 44/26/38），
**前两张精确命中，包括 docling 错的那张**。

→ 把 plumber 的行数接进 `audit` 当第二意见，比"换引擎"便宜得多，且不动现有产物。
`AGENTS.md` 的 confidence 契约本来就写着「high = 两个独立引擎的**行列数**一致」，
**行数那一半从来没实现过**（本轮 A4 已把这处落差记进文档）。

### 29.3 一条要防的误读：互补 ≠ 可替换

plumber 在 `pbc_21296` 上完胜（44 行 vs 43、忽略空格 100%），很容易得出"换成 plumber"。
但同一批表里 `pbc_28772` 它合并了两列、把折行单元格拆成两行，`pbc_26870` 直接测不出。
**必须把三张表一起看** —— 只看赢的那张就会做出错误决策。

### 29.4 方法论上最值钱的四条（比数字更耐用）

1. **一次只测一个方法。** 混着测出来的第一版报告里，plumber 一列全是 `—`、OCR 全是 `0`
   —— **等于没测却看着像测过**。用户当场指出。这是本轮最大的返工。
2. **「测不出」必须与「0 分」分开写。** 前者要写原因，后者才是分数。
3. **只看 `check_cols` 会漏掉整类错误。** plumber 那处切列错落在非 check_col 的
   `Median final RTV` 上。`check_cols` 是给"目标 (b) 准确率"用的，不是"引擎质量"的全部。
4. **0 分母长得像"没数据"。** 图片表打分第一次跑出"gold 0 个响应码"，
   实际是 `#N` 列名在 gold 正文里查无此列、**读取时就丢了**。不报错、不崩溃，最难发现。

### 29.5 四步的产物

探针三个：`spike/probe_engine_docling.py` / `probe_engine_plumber.py` / `probe_engine_paddle.py`
（外加 §25 的 `probe_row_bands.py`）。`eval/gold.py` 新增 `ignore_spaces` 档与 `#N` 列名解析。
**`pdf_table_extract/` 全程一个字节没动**（每步 md5 比对）。

### 29.6 第 4 步验收

3 条专属 + 3 条共通全过。loop 到此结束。

## 30. spike 验证候选 A / B（2026-07-27）—— 两个都通过，但都不是第一版

用户定的范围：**只做 spike 验证，效果确认了才写回产品。** 本节两个候选，
`pdf_table_extract/` 一个字节没动。数字见 `eval/findings.md` 的「spike 验证」一节。

### 30.1 候选 A（坐标拆分）：**行数修好、7 张表零回归，但 gold 的 100% 是虚高的**

`pbc_21296` p4 43 → **44** 行；gold 打分 84 计分/漏 2 行 → **88 计分/漏 0 行/100%**；
其余 7 张单页文字表行数**全部不变**。

**但逐列核对发现 10 列里有 1 列丢了值**：`KM estimate` 的 `> EP > EP` 拆成了
`>EP` + `（空）`。而 **`KM estimate` 不在 `check_cols` 里，所以 gold 打分完全看不见这个丢失**。

→ **第 2 步那条教训第二次应验**：`check_cols` 是给"目标 (b) 准确率"用的，
**不能拿它当"修法完整性"的证明**。差点就凭 100% 宣布修法无损。

→ 因此建议写回产品时**同时加一道自检**：拆分后校验「拆出各格字符总数 == 原合并格字符数」，
不等就打 `split_lossy`。零成本、自洽，正好抓住上面那一格。
**不加就等于用一个静默的错换掉另一个静默的错。**

顺带：拆出来的 `Histology` 是 `ALL T-cell`，**空格比 docling 原来的 `ALLT-cell` 还准** ——
因为它是从词坐标重建的，而不是 docling 拼的。

### 30.2 候选 B（断行连字符）：**第一版 5 种命中里 3 种是假阳性**

纯正则 `([a-z])-\s+([a-z])` → `\1\2` 会把这些改坏：
`lineage- defining` → `lineagedefining`（合法复合词）、`patient- derived` 同、
`Log-rank p- vaiue`（`p-value` 的连字符该留）、还有一个 **URL** `childhood-cancer- statistics/`。

**surface pattern 完全相同，正则分不开。** 真断行拼接后是**一个真词**，
合法复合词拼接后是不存在的字符串 → 判据必须去文字层查一次。

收紧后 5 种只改 1 种（正是目标），gold 对齐率 89% → **100%**、漏行 5 → **0**。

**踩坑，而且是自己给自己下的套**：收紧版第一次实现时我在**建词表时**也做了
`re.sub(r"-\s+", "")`，把词表里的连字符一并接掉 → `lineagedefining` 凭空进了词表 →
**判据自己证明自己**，假阳性照旧。改成「按非字母切词、不预先接连字符」才对。
**建参照集时做了和被检对象相同的变换 = 循环论证**，这个形状要记住。

### 30.3 两个候选的共同点：**第一版都错，都是靠"回归影响"那一栏抓出来的**

A 的第一版差点凭 gold 的 100% 过关（靠逐列核对才发现丢格）；
B 的第一版会改坏 3 种合法内容（靠全语料枚举才发现）。

→ `probe_candidate_fixes.py` 那个「修复效果 / 回归影响」双栏范式**不是形式主义**。
只看修复效果，两个候选都会带着 bug 进产品。

## 31. spike 验证候选 C（图片表）：**C2 净效果为负，C1 无差别**（2026-07-27）

### 31.1 C2 逐格 OCR：表头大赢，数据行全崩

| 表 | 响应码重叠 基线 → C2 | C2 非空行 |
|---|---|---|
| `pbc_24724` | 83% → **2%** | 10/47 |
| `pbc_21296` | 89% → **0%** | 2/47 |

**表头那一半是真赢**：`Xenograft Line` 完整回来了（基线是 `Histology`，整表左移），
`Median Score` / `Midpoint Difference` / `Overall Group Response` 全对，
**连 F-005 的列错位都消失了**。但**数据行 37/47 与 45/47 全空** → **净效果为负，整体不采用。**

### 31.2 根因是结构性的，不是调参问题

数据格实测高 **中位 23px（min 22）**，表头格高得多。六种配置全试过：

- 四周 pad=10 + 2x / 4x → **6/6 空**
- 四周 pad=20 + 4x → 有输出，但**把上下邻格一起读了**（`'D045 D456 NB-SD'` 是 3 个格的内容）
- **只加水平 pad**（3 种组合）→ **1/6 非空，且那个还是错的**
- 不加 padding 只放大 4x/6x → **1/6**

→ **rapidocr 的检测阶段在"只含几个字符的小图"上找不到文字，与分辨率无关。**
四周 padding 能让它工作但必然吃邻格 —— **两条路互斥，无解。**

### 31.3 ⚠ 上一轮我是怎么被 5 个样本骗的（这条比结论重要）

探针只测了 **5 个表头格**，我据此把逐格 OCR 写成"潜力最大的方向"。
**表头是最容易的一档**（格高、文本长），数据行（22px 高、值 2–3 个字符）**完全相反**。

**本轮第三次踩"样本不够就外推"**：
① F-001 留出集 0 命中被当成"不是普遍缺陷"（后来自查修正）；
② 序列对齐只有 1 张表；
③ 这次的 5 个表头格。
→ **同类样本数 ≤5 时，禁止写"最有潜力/最该做"这类判断。**

### 31.4 C1 wireless 检测器：实质无差别

300 → 299 格、281 → 276 格。以为三线表（无竖线）用 wireless 更对路 ——
**差别在噪声范围内**。回退零成本，但也没收益。**不采用。**

### 31.5 C3 没测出生产配置下的效果（我的设置错了）

`pbc_24724` 我喂的是**未裁剪**图，整表还原直接失败（符合 PDF 事实 #9），
**不是生产配置**（生产先按密集带裁剪）→ 这张的 C3 效果**测不出**。
`pbc_21296` 生产产物里本来没有全空行，删了没变化。
→ **C3 待用生产配置重测**，不能凭本轮结果下结论。

## 32. 留出集泛用性检验：A 不通过、B 通过（2026-07-27）

用户要求把 A/B 拿到留出集上验。**判据与常数与开发集完全一致，本次未做任何调整。**

### 32.1 A 在留出集上 **14 处拆分全错**

| | 开发集 | 留出集 |
|---|---|---|
| 检测命中 | **1 / 28 张表** | **28 / 27 张表** |
| 拆分动作 | 1（正确） | **14（全错）** |

误拆全是同一形态：**一个格子的文字折成多行，被拆成多个数据行**。
最刺眼的是 `Chen p11 行7`——`Receptor tyrosine kinases` 被拆成
`Receptor` / `tyrosine` / `kinases` 三行，外加 2 个全空行。

**根因**：判据 `>2 列有多带 ⇒ 压行` 依赖一个**开发集特有的性质：行高整齐**。
`pbc_21296` 首列高度 8.48/8.52/8.65/8.69/8.77 加一个 **15.78** —— 1.85× 是**孤立离群点**。
而留出集的 `Bruhm`/`Chen`/`Faupel-Badger` 是**文本型综述表**，本来就多列同时折行、
行高天然参差，判据完全分不开。

**这是"正例只有 1 个"那条风险的直接兑现，而且兑现得非常快。**
我在前面几节里反复标注这条局限，但仍然低估了它 —— **标注风险不等于降低风险**。

### 32.2 B 在留出集上 3/3 正确不动

`lineage- defining`、`patient- derived`、一个 URL 全部正确放过。
两个语料合计 **8 个决策全对**（1 接 + 7 不动）。

**但留出集里一个"该接"的正例都没有** → B 的「接」分支**总共仍然只有 1 个正例**。
泛用性只在**不误伤**这一侧得到验证，**接对**那一侧没有。这个不对称要写清。

### 32.3 教训：开发集的"结构同质性"是个隐形前提

A 的判据从没写过"要求行高整齐"，但它**隐含依赖**这一点 ——
因为开发集 12 篇全是 PDX 实验表（数值密集、行高统一）。
留出集是单细胞/基因组学综述，表的形态完全不同。

→ **下次定判据时要显式问一句：这条判据依赖开发集的哪个隐含性质？**
这比事后在留出集上被打脸便宜得多。

### 32.4 一处文档不一致（用户 2026-07-27 澄清）

`AGENTS.md` 写「留出集是 test set，不是 validation set / 跑完不许调参」，
而用户说明**它就是专门用无关文章来测阈值的**，允许据此调。
两处不一致，**`AGENTS.md` 该改** —— 否则下一个 session 会按另一套规矩走。
（已在 `probe_fix_candidates.py` 的 holdout 段注释里记下这条差异。）

### 32.5 A 误拆的真正病因：在「全框线表」上用了启发式（用户指出）

用户问「Chen 那张表明明有很清楚的线，为什么还会错」——一句话戳中。
**我的拆分器根本不看线。**

实测「去重横线数 / 行数」：误拆的三张全是 **111–114%**（每行都有边框线），
A 唯一拆对的 `pbc_21296` 是 **7%**（三线表）。**完美分离。**

**但局部判据不成立**：`ALL-8`/`ALL-16` 之间也没有线（三线表没有行线），
所以「两摊字之间有没有线」会把真阳性一起杀掉。**必须在表的层面先分类**：
全框线表读线、三线表才用启发式。阈值 7% ← 90% → 111%，
中间 `pbc_28772` 的 45% 是**分组线**（只有 1 个样本）。

**这条再次印证同一个原理**：A/B 成立是因为引入了一份没读的事实（词坐标 / 文档词表），
这次失败同样是因为**还有一份没读的事实（排版画的线）**。
→ **判据失效时先问"还有哪份现成事实没用"，而不是"再想个更聪明的规则"。**

（阈值是在留出集上量的 → 留出集对它不再是独立证据；且 45% 那档只有 1 个样本。**未实现。**）

### 32.6 用户提的「行高均匀度」闸门实测成立，余量 16 倍

用户提议先判表的行高分布是否均匀，均匀才启用 A。**这比我提的线判据更贴近病根。**
（一处修正：闸门放不到 docling 前面 —— 要量的高度就是 docling 的 cell bbox。）

实测「同列、排除可疑行后的 极差/中位」：**该拆 6.5–7.1%；不该拆最小 13.9%、多数 92–409%。**

**设计细节决定成败**：必须"**所有**超标列都整齐"。反例 `Bruhm p3 行10` 列0 只有 13.9%
（按"任一列"会放行），但列3 是 138.4%。改成取该行所有超标列的**最大**离散度后：

**7.1% ← 阈值 → 114.8%，16 倍余量；14 处误拆全挡、唯一真阳性通过。**

比我的线判据更可取：直接量 A 依赖的那个假设本身、不用额外抽线、
不受"分组线"中间态（`pbc_28772` 的 45%）干扰。

**诚实标注**：阈值在留出集上量的（该集对它不再独立）；真阳性仍只有 1 个
（闸门解决的是**误报**，"拆得对不对"那侧样本没增加）。

### 32.7 A 加上行高均匀闸门后：留出集误报清零，开发集真阳性完好

用户判断「A 的逻辑合理，只是不适用于所有表格」——按这个思路把闸门加进**检测器**
（不是拆分器：因为"比中位高 1.5×"的**前提**就是同列高度整齐，前提不成立时信号本身无意义）。

`HEIGHT_EVEN_MAX = 0.30`，取 7.1% 与 114.8% 的几何中心（28.6%），两边各约 4 倍。

| | 加闸门前 | 加闸门后 |
|---|---|---|
| 留出集 检测 / 拆分 | 28 / **14（全错）** | **0 / 0** |
| 开发集 检测 / 拆分 | 1 / 1（对） | **1 / 1（对）** |
| 开发集 gold | 88/88、漏 0 | 同左 |

探针会同时打印「不加闸门会命中多少」，**证据自带在产物里**。

**证不出来的那一半**：留出集加闸门后检测数为 0，所以**无法证明闸门没造成漏检** ——
那批料里没有已知的真压行样本。**"消除误报"有 14/14 的证据，"不误杀"只有 1 个正例。**

### 32.8 这一轮的整体形状值得记

三个候选，最后 **A（加闸门后）和 B 可以写回产品，C 不能**。而三个候选**第一版全是错的**：

- A 第一版：开发集通过、**留出集 14/14 误拆**
- B 第一版：修复效果好、**回归栏 5 种里 3 种改坏**（含一个 URL）
- C：表头大赢、**数据行全崩**

**每一次都是"另一栏"抓出来的**，不是"修复效果"那一栏。
而每一次的修法都不是"想个更聪明的规则"，是**引入一份原本就在、只是没读的事实**：
词坐标 / 文档词表 / 行高分布。**C 之所以救不了，正是因为它没有这样一份事实可读。**

## 33. A2 与 C2：两条新路线（2026-07-28）

用户提的两条：**A 能不能用 pdfplumber 的文字来修正 docling**、**C 能不能试逐行 OCR**。
两条都测了，结论一正一负。数字见 `eval/findings.md`「A2 与 C2」一节。

### 33.1 A2 比 A 好，但仍不够（corpus3 上 3 通过里只有 1 对）

设计是**外科手术式**：只对检测器标出的那一行去 plumber 网格里找对应行组，
并做**逐列字符自校验**。不是引擎融合。

开发集 1/1 对，而且**比坐标拆分多修好一格**（`>EP` 两行都在，10/10 vs A 的 9/10）。
但 corpus3 上 22 处检测、3 处通过自校验，**只有 1 处是真的**。

**根因是原理性的**：字符自校验只能保证"一个字没丢没多"，
**分不开「两行压成一格」和「一个值折成两行」—— 两者拼起来完全一样**。

**踩坑**：第一版整串比对必然失败 —— docling **按列**拼、plumber **按行**排，
字符顺序不同。必须逐列比。

### 33.2 C2 逐行 OCR：**300dpi 大赢，120dpi 更差** —— 印证了"两张图片表是两种病"

| 表 | 基线 | C2 逐行 | 耗时 |
|---|---|---|---|
| `pbc_24724`（300dpi） | 83% | **98%** | 15.8s |
| `pbc_21296`（120dpi） | 82% | **41%** | 12.9s |

**300dpi 那张连 F-005 的整表左移都修掉了**（首列从 `Histology` 恢复成 `BT-29`/`KT-11`）。

**为什么逐行能成而逐格不能**：格子 22×132px **两个方向都小**，加 padding 才能被检测到
但必然吃邻格 —— 两条路互斥；而**一整行是 22×900px**，正是单行文本的形状。

**120dpi 更差的原因**：`['1 BT-29','Rhabdod','3.0','0-20 PD2',…]` ——
OCR 把相邻格的字识别成**同一个词框**，字糊了词框就不可信，按 x 切列切不开。

**成本白赚**：基线整表还原约 18s，C2 是 12.9–15.8s，**更快**。

### 33.3 这一节最值得记的判断

`log.md` §31.3 立过一条规矩：「同类样本 <=5 个时，禁止写"最有潜力"这类判断」。
这次 C2 只有 2 张表，所以**不能说"逐行 OCR 是对的方向"** ——
只能说「在 1 张 300dpi 的表上从 83% 到 98%，在 1 张 120dpi 的表上从 82% 掉到 41%」。

分流判据（按 `ImageInfo.effective_dpi`）现成就有，但**两档各 1 个样本，阈值无从定**。

### 33.4 用户提的「用右侧轴标签当第二次读数」—— 实测强验证，但样本仍只有 2 张

右半边轴标签的识别质量**远好于表格首列**：`pbc_24724`（300dpi）右侧
`BT-29/KT-11/KT-13/SK-NEP-1/EW5/TC-71/CHLA258/Rh28…` 几乎零错，
而产物首列因整表左移**整个丢了**；`pbc_21296`（120dpi）右侧有 **4 个完全正确**
（`KT-16`/`KT-14`/`KT-10`/`KT-11`）、多数只差一个字符，而表格首列基本全丢首字母。
耗时 **0.4–0.7 秒**。

**机制与 C2 逐格失败同源**：轴标签在空旷区域、正常字号、四周无邻格文字；
表格格子挤在一起，检测器分不开。

泛用表述（不含领域知识）：「同一张图里若另有区域的文本块 y 位置与表格行带一一对应，
就当作行标识的第二次读数」。没有配对区域时自动无操作。

### 33.5 一个必须承认的缺口：C2 只在开发集 2 张图上测过

用户问留出集泛化性 —— **没测**。而且留出集的 9 张图片表**没有 gold**，
所以即使跑了也只能测"两法是否一致"，**测不出谁更准**。

→ `83%→98%` 的样本量是 **1 张表**。按 §31.3 的规矩，
现在只能说「在这 1 张 300dpi 表上成立」，不能说"逐行 OCR 是对的方向"。

**A/A2 的拆分、C2 的 dpi 阈值、右侧轴标签这条 —— 三件事卡的都是同一个瓶颈：
图片表与横向压行表的 gold 太少。** 这比再想新判据更值得投入。

## 34. 低分辨率图片表：把外部建议逐条量掉，结论**推翻了它的第一名**（2026-07-28）

用户拿到两份外部（另一个 AI）的建议清单，核心排序是 **① 超分辨率 ★★★★★、
② 不要整表 OCR 要逐 cell ★★★★★、③ CLAHE + 自适应二值化 ★★★★☆、
④ 逐行切分 ★★★★☆、⑤⑥⑦ 换识别模型 / MinerU / OCR 集成 ★★★★☆**。
本节把能零安装量的全部量了一遍，语料是我们唯一两张图片表
（`pbc_24724` Fig.1 = 300dpi，`pbc_21296` Fig.1 = 120dpi）。

新增三个探针：`spike/probe_lowres_ocr.py`（检测器参数）、
`spike/probe_lowres_prep.py`（预处理 + 像素预算）、
`spike/probe_lowres_confirm.py`（`--phase2` 框放大 / `--phase3` 降采样对照 /
`--phase4` 逐行×CLAHE / `--phase5` 逐行的分辨率下界）。**产品代码一行未改。**

### 34.1 打分改用「参照命中」—— 这是本节能得出结论的前提

以前量图片表用的是"响应码重叠率"，它有个致命盲区：**格子变空会让比率上升**。
实测 `pbc_24724 +CLAHE+unsharp` 报 91%，看着比 baseline 的 69% 好得多，
真相是那一列非空格子从 36 掉到 20 —— **丢了 14 个值，比率反而涨了**。

换成两个指标并列，缺一不可：
- **参照命中** = 图内首列有多少个标签与**同篇文字表**首列**逐字相同**（文字层来的，可信）
- **合法码 n/总** —— 分子分母都报，比率单独看无意义

### 34.2 真正的错法不是"认错字"，是**词首字符被吃掉**

把首列原样打出来才看清（以前只看比率，所以一直没发现）：

| 真值 | 整表管线读成 |
|---|---|
| `BT-29` | `8T-29`（21296）/ `Y-29`（24724） |
| `KT-11` | `T-11` |
| `SK-NEP-1` | `NEP-1` |
| `EW5` | `W5` |
| `TC-71` | `-71` |
| `CHLA258` | `A258` |
| `Rh10` / `Rh28` | `s10` / `28` |

**这是检测框左边界切进了文字里，不是识别错。** 两个分辨率上都一样 ——
所以它跟分辨率无关。这条一旦看清，后面所有结论都跟着变。

### 34.3 逐条结果

| 外部建议 | 我们的实测 | 结论 |
|---|---|---|
| ① 超分辨率 ★★★★★ | 见 34.4，**它排第一是错的** | ❌ 本轮不装 |
| ② 不要整表 OCR，先检测 cell | **我们早就在做**（PDF 事实 #9/#9b，`TableCellsDetection` 绕过版面分类）；它说的 PP-TableMagic = 我们用的 `TableRecognitionPipelineV2` | ✅ 已在产品里 |
| ③ CLAHE | **120dpi 上参照命中 5→18/43**（+13），换裁剪边距 0.02/0.04 结果**完全一致**、8 组检测参数下也一致 | ✅ 真效果，零安装 |
| ③ 自适应二值化 / Otsu | 120dpi 合法码 84%→**52%/51%**；300dpi 首列码列崩成 `-O.501`/`0 007` | ❌ **有害，方向性错误** |
| ③ 去噪 fastNlMeans | 120dpi 合法码 84%→**93%**（41/44），参照 5→11 | ✅ 有效但与 CLAHE 不叠加 |
| ③ unsharp | 参照 5→7，合法码 84%→68% | ➖ 净负 |
| ④ 逐行切分 | **300dpi 参照 0→32/41、合法码 69%→98%** | ✅ 最大的单项收益 |
| ⑤ 换识别模型 | `en_*_server_rec` **不存在**（早先已测）；逐行用的 RapidOCR(PP-OCRv6) 已经是第二个引擎 | ➖ 已尽 |
| ⑦ OCR 集成投票 | 逐行 OCR 已经引入 RapidOCR，与 paddle 整表管线**结论相反**（0 vs 32）—— 此时"投票"没有意义，是**其中一个系统性错了**，不是随机分歧 | ❌ 前提不成立 |
| 调 `unclip_ratio` 防粘连 | 见 34.5，**方向反了** | ❌ |

### 34.4 降采样对照实验：**超分排第一是错的**（本节最值钱的一条）

问"120dpi 读不出来是信息没了还是管线不行"，光看那张图分不开 —— 没有对照。
做法（`--phase3`）：拿 **300dpi 那张信息充足的图**人工降到 200/120/100dpi 再跑一遍。
同一张图，唯一变量是分辨率，所以有真值。

整表管线在这条降采样序列上的参照命中：

| | 300dpi(真值上界) | 200dpi | 120dpi | 100dpi |
|---|---|---|---|---|
| 原生每行像素 | 27.8 | 18.5 | 11.1 | 9.3 |
| 参照命中 /41 | **0** | 2 | 0 | 4 |
| 合法码 | 69% | 92% | 95% | 39% |

**300dpi 原图的首列成绩（0/41）比它自己降到 100dpi（4/41）还差。**
即：`pbc_24724` 的首列错**完全不是分辨率问题**。给它再多像素也不会好 ——
"完美超分"能拿回的上限就是 0。**超分对这张表的收益上界为零，实测出来的，不是推理。**

反过来说超分也没被完全否掉，但目标窄得多，见 34.6。

### 34.5 检测框放大：方向反了，而且调大会**丢数据**

外部建议调低 `unclip_ratio` 是治"两行粘连"；我们看到的是"框太小吃掉首字符"，
所以试了调**高**（`--phase2`，1.5/2.0/2.5/3.0）与 `box_thresh` 调低（0.4/0.3）：

- `box_thresh` 0.6→0.4→0.3：**输出逐字节不变**，8 组里 6 组完全一样
- `unclip=2.0`：21296 参照 5→7（微弱），合法码 84%→80%
- `unclip=3.0`：合法码报 **100%** —— 但是 **7/7**，那一列从 44 个值掉到 7 个。
  **又一次"格子变空推高比率"**，34.1 那个坑在参数扫描里重演了一遍
- 24724 上 8 组参数**全部 0/41**，纹丝不动

还顺带否掉了我自己早先的一个判断：我量到 `TableRecognitionPipelineV2` 内部把长边压到
**960**（`text_det_limit_side_len=960/max`），据此说"再放大也白搭"。
**那句话的前提是错的** —— 960 是 `predict()` 的入参，可以直接改。
但改了没用：`side_len` 1536 / 2048 在 21296 上把合法码从 84% 压到 **77%**，
`limit_type=min` 反而升到 91%。**结论没变，理由变了** ——
不是"改不了"，是"改了不解决问题"，因为瓶颈不在这一层。

### 34.6 逐行 OCR 的分辨率下界：**原生每行像素**，不是 dpi

`--phase4` 的 2×2（逐行 × CLAHE）：

| | 参照命中 /41 或 /43 | 合法码 n/总 |
|---|---|---|
| **24724** 整表（产品现状） | 0/41 | 25/36 = 69% |
| **24724 逐行** | **32/41** | **40/41 = 98%** |
| 24724 逐行 + CLAHE | 31/41 | 39/41 = 95% |
| **21296** 整表（产品现状） | 5/43 | 37/44 = 84% |
| **21296 整表 + CLAHE** | **18/43** | 37/44 = 84% |
| 21296 逐行 | 16/43 | 18/**20** = 90% |
| 21296 逐行 + CLAHE | 17/43 | 26/**29** = 90% |

**两个分辨率的最优解不同**，而且原因清楚：
120dpi 上逐行 OCR 把响应列 44 个值读成 20~29 个非空（RapidOCR 打了 7 次
`text detection result is empty`），参照命中虽然也涨到 16~17，但**代价是丢一半数据**；
整表 + CLAHE 拿到 18 个参照命中**且 44 个码一个不丢**。

拐点用 `--phase5`（同一张图降采样，逐行 OCR）量出来：

| 原生每行像素 | 27.8 | 18.5 | 13.9 | **11.1** | **9.3** |
|---|---|---|---|---|---|
| 参照命中 /41 | 32 | 32 | 22 | 31 | **4** |
| 合法码 | 98% | 98% | 90% | 90% | **29%** |
| 非空格子 | 203 | 199 | 191 | 173 | **138** |

**悬崖在 11.1 ← → 9.3 之间**，三个指标同时掉。而真实的 `pbc_21296` 是
**8.0 px/行**，落在悬崖下面 —— 与它逐行 OCR 表现差**互相印证**（两个独立样本同向）。

**阈值：原生每行 ≥ 12 px 走逐行 OCR，否则走整表 + CLAHE。**
- 余量：11.1 通过 ← 12 → 9.3 失败，**只有 1.1~2.9 px，非常薄**
- **单点拟合**：拐点只有一条降采样曲线（一张图），别的图上拐点在哪不知道
- 好处是这个量**零成本**：`cells_summary` 已经免费给了 `n_row_bands`，
  原生高度也已知，`px_row = 原生高 / 行带数` 一行算完

### 34.7 未决 / 已否决，以及否决理由的性质

- **超分辨率（Real-ESRGAN 等）：未决，但优先级降到很低。**
  否决理由是**选择性不够 + 收益上界已知为 0（对 24724）**，不是"技术做不到"。
  对 21296（8.0 px/行）它是理论上唯一能跨过 12px 悬崖的手段（需要 ~1.5× 真细节），
  但三条挡着：(a) 要往共享 `gemini` 环境装 torch + basicsr，须先问用户；
  (b) 文献明确记载文本超分的失效模式是**幻觉笔画**（sharp strokes that alter glyphs），
  而 PDF 事实 #11 说得很清楚 —— 一个看起来合法的错码是我们**最危险的一类错误**；
  (c) 21296 的身份列在**同篇文字表里 43 个标签 100% 完好且免费**，超分再好也只是追平它。
  另外外部建议推荐的 `RealESRGAN_x4plus_anime_6B` 是**动漫权重，不该用在文档上**。
- **VLM / LLM 后处理（Qwen2.5-VL / GOT-OCR2.0 / DeepSeek-OCR / MinerU）：否决。**
  理由是**选择性够但违约**：`pdf_table_extract/` 的非目标写着不联网不调 API，
  且本地 VLM 的卖点恰恰是"根据上下文推断模糊字符" —— 那是**铁律 #1 的反面**，
  在 PDX × 响应表上推断出的 `PD1`/`PD2` 是静默数据错误。`eval/` 侧已有 LLM 当第二读者。
- **OCR 集成投票：否决，前提不成立。** 投票要求各引擎的错是**独立特异**的；
  实测 paddle 整表 0/41 vs RapidOCR 逐行 32/41 —— 这不是分歧，是一个系统性错了。
  该做的是**选对路径**（逐行），不是给错的那条投票权。

### 34.8 踩的坑

1. **比率单独看会骗人**，而且骗了两次（预处理扫描、参数扫描各一次）：
   格子变空 → 分母变小 → 比率上升 → 看起来像改进。**分子分母必须一起报。**
2. **管线对裁剪框极其敏感**：插值方式对照里裁剪框差 2 个格子（280 vs 282），
   参照命中就从 5 掉到 0。所以 CLAHE 的 5→18 **必须换边距重测**才敢信
   （测了 0.02/0.04，两个边距逐字节相同，才算过）。
   凡是只跑一次裁剪就下的结论，都不可信。
3. **我早先"960 压缩所以放大无用"的说法，前提是错的**（见 34.5）。
   结论侥幸没错，但**理由错了会让后人在错误前提下重开讨论** —— 记在这里。
4. **只看比率就永远发现不了 34.2 那个"首字符被吃掉"**。
   诊断类指标必须能打印原始值，不能只留一个数。

## 35. 留出集图片路径首次全量抽取：**§34 的优先级被推翻**（2026-07-28）

用户问"你在留出集测过吗"。答案是**没有**，而且 `holdout/` 结构上就测不了 ——
它只跑 `--list`、不走抽取（`AGENTS.md` 已写明这是它的盲区）。
§34 那些结论全部建立在**开发集的 2 张图片表**上，那条看着有 5 个点的降采样曲线
也**全部来自同一张图**。

所以这次真跑了：留出集 25 篇（成功 24 篇）全量 `--all` 抽取 + LLM 当独立第二读者。

### 35.1 先踩了一个会导致**假阴性结论**的坑

第一版扫描脚本用了 `timeout 600 conda run ...`。**macOS 没有 `timeout` 这个二进制**，
25 次全是 `exit 127`，输出目录空的。
如果我照着"空目录"下结论，就会写成「留出集里没有图片表，所以测不了」——
**一个彻底错误、而且看起来非常合理的结论**。

抓到它靠的是**阳性对照**：拿已知有图片表的 `pbc_24724` 跑同一套流程，
确认能抓到 `fig_1`。**以后凡是"某语料里 0 命中"的结论，必须先跑阳性对照。**
这条比本节任何一个数字都更该记住。

### 35.2 留出集 24 篇的图片路径真实战果

| 结局 | 条数 |
|---|---|
| ✗ 检出网格但**结构还原失败** | **108** |
| — 闸门判为纯图表（跳过 OCR） | 83 |
| △ 出了 CSV 但是 **1 行 0 列**的退化输出 | 5 |
| ✔ 出了非退化的表 | **3** |
| ✗ 抠原生图报错 `ValueError` | 3 |

**199 条候选，最后只有 3 条产出了像样的表。**

### 35.3 LLM 判读（分层抽样 23 条）—— 分母终于有了

光看 35.2 那张表下不了结论：108 条失败里可能全是热图/多面板图，
那"失败"就是**正确行为**（PDF 事实 #9b：检测器会在柱子和网格线上检出伪单元格）。
所以按用户建议用 LLM 当独立第二读者，只问一件事：**这块区域里有没有一张数据表？**
FAIL 那 108 条按检出单元格数**分层抽样** 15 条（20~300 格，等距取，
直接取前 N 会全是小网格、取后 N 会全是 300 格封顶的热图）。

| 组 | 送检 | LLM 判定**是表** |
|---|---|---|
| ✔ OK | 3 | **2** ← 有 1 个是**误报** |
| △ DEGEN | 5 | **5** ← 全是真表，却输出了 1×0 |
| ✗ FAIL | 15（抽自 108） | **4**（27%） |

推算：108 × 27% ≈ **29 张真表在"结构还原失败"里丢掉了**。
加上 5 张退化的、2 张成功的，留出集里大约有 **36 张真图片表，我们只正确产出 2 张 ≈ 6%**。

具体例子（LLM 读出来的，可回查 `/tmp/pte_img_holdout_gold.json`）：
- `Bielski_Taylor_2021 p4`（检出 31 格→失败）：真表，`WT copies / Mutant copies / Loss of WT / Mechanism`
- `Bao 2025 p5`（113 格→失败）：真表，10×10 混淆矩阵，首列 `Prostate/Pancreatic/Lymphoma…`
- `Bie 2023 p8`（169 格→失败）：真表，7×7 混淆矩阵，首列 `BRCA/NSCLC/ESCA…`
- `Baslan 2022 p26`（298 格→失败）：真表，生存曲线下方的 `Number at risk`
- **误报**：`Chen 2024 p5 fig_2` 我们输出了 `10×21`，LLM 说那是
  「基因 × 肿瘤类型的**色块矩阵**，灰/白格子表示是否受累」——**不是表**

### 35.4 结论：§34 提的两条修法**解决不了留出集的主要问题**

§34 建议「≥12px/行 走逐行 OCR、<12px 走整表 + CLAHE」。
这两条改的都是**已经能还原出网格的表**的识别质量。
而留出集的主导失败是 **`TableRecognitionPipelineV2` 结构还原直接返回空**（108/199）。
**在还原失败的表上，识别质量再高也没有东西可识别。**

→ 所以优先级要倒过来：
1. **先解决"还原失败"**（留出集 108 条，其中约 27% 是真表）
2. 再谈 CLAHE / 逐行的识别质量（开发集 2 张已验证有效，留出集尚未验证）

**逐行 OCR 有可能同时解决第 1 条** —— 它**绕开 `TableRecognitionPipelineV2` 的结构还原**，
直接用单元格检测的行带/列带自己搭网格。也就是说它可能在"还原失败"的地方照样出表。
这是下一步要测的，测法见 §36。

### 35.5 两个**新的产品缺陷**（都是留出集才暴露的）

- **F-019 退化输出**：5 条产出了 `1 行 0 列` 的 CSV，而 LLM 说 **5/5 都是真表**。
  1×0 比失败行更糟 —— 失败行是"我没做到"，`1×0` 的 CSV **看起来像做到了**。
  开发集 12 篇零出现。
- **F-020 色块矩阵被当成表**：`Chen 2024 p5` 输出 `10×21`。
  单元格检测器在纯色块矩阵上照样检出规整网格，闸门（≥20 格）拦不住，
  形状也自洽 → `grid_shape_mismatch` 不响。图片路径**没有任何判据能识别"格子里没有字"**。

### 35.6 关于这份 LLM gold 的效力边界（必须写清，否则会被高估）

- LLM 读图**本身也是一种 OCR**，低分辨率上一样会错。它**不能当逐格真值**。
- 它值钱的地方是**不与我们的 paddle 管线共享失败模式**（不同模型、看整页、有上下文），
  所以「**是不是表**」这个二分判断可以当独立证据用。逐格数值不行。
- 抽样是 15/108，**比例有抽样误差**；分层抽样只保证覆盖了各个单元格数档位。
- `eval/` 允许调 LLM（AGENTS.md 非目标里的明确例外），**产品本体一行网络代码都没有**。

## 36. 逐行 OCR 能把「结构还原失败」救回来 —— 它的价值不是提质量，是**从 0 到有**（2026-07-28）

§35 留下的待测项。测的是 LLM 确认是真表、而当前抽不出来的 9 条
（4 条 FAIL「结构还原失败」+ 5 条 DEGEN「输出 1×0」）。
逐行 OCR 绕开 `TableRecognitionPipelineV2` 的结构还原，
改用单元格检测的行带/列带自己搭网格。

`spike/probe_img_rescue.py`。判据只比**首列**、不比逐格 —— LLM 读图自己也是 OCR，
逐格数值不能当真值（F-021 的效力边界）。

| 论文 | 页 | 当前 | 逐行 OCR | 首列命中 | 判 |
|---|---|---|---|---|---|
| `Bie 2023` | 8 | 0×0 失败 | **9×9** | **7/7** | ✅ 干净 |
| `Bao 2025` | 5 | 0×0 失败 | **12×10** | **8/10** | ✅ 干净 |
| `Islam 2024` | 18 | 1×0 退化 | **4×5** | 2/3（第 3 个 gold 是空串） | ✅ 干净 |
| `William 2021` | 4 | 1×0 退化 | 17×12 | 5/5 | ⚠ **部分** |
| `Beddows 2023` | 26 | 1×0 退化 | 有产出 | 部分 | ⚠ **部分** |
| `Bielski 2021` | 4 | 0×0 失败 | 2×1 | 0/4 | ❌ |
| `Baslan 2022` | 26 | 0×0 失败 | 19×35 | 0/3 | ❌ |
| `Rozenblatt 2020` | 4, 8 | 1×0 退化 | — | — | 该页无 PictureItem，未计入 |

**按"首列标签命中过半"这条判据是 5/7；但逐条看结构，只有 3 条是干净的表。**
差别在哪必须写清，否则数字会被高估：

- `Bie 2023 p8` 出 `9×9`，前几行是 `['','15','8','2','0','']` / `['','2','32','4','','2']`
  —— 混淆矩阵的数值行，**结构对**
- `Bao 2025 p5` 出 `12×10`，`['Pancreatic','4','2','3','','6 1']` —— **结构对**
- `Islam 2024 p18` 出 `['Chen et al.','E8.5 embryo2','19019','intBCs: 2788',…]` —— **结构对**
- `William 2021 p4` 虽然 5/5 命中，但网格是 `['A','CD3+','','','CD8+','']` /
  `['10.0','0 0',…]` —— **它把整张多面板图（含散点图）当成一张表切了**。
  标签确实被认出来了，但**行列对应关系是假的**。
  这暴露我这条判据的弱点：「标签出现在网格里」≠「标签在正确的行上」

**没救回的两条各是一类新问题**：
- `Bielski 2021 p4`：`extract_native_image` 抛 `ValueError`。
  留出集普查里另有 3 条同样报错 —— **抠原生图有一类未处理的失败**，独立于 OCR
- `Baslan 2022 p26`：单元格检测 **298 格**（接近 300 上限），出 `19×35` 的垃圾网格。
  那是整版多面板图，真表（`Number at risk`）只占其中一小块 ——
  **必须先把区域缩到表那一块**，密集带启发式（PDF 事实 #9b）在这里没起作用

### 优先级修正（这是本节的结论）

§34 把逐行 OCR 定位成"提高识别质量"（24724 从 83% 到 98%）。**定位错了。**
它真正的价值是**在当前返回空的地方产出表** —— 留出集 108 条结构还原失败里
约 27% 是真表（≈29 张），逐行 OCR 至少能碰到其中一部分。
"从 83% 到 98%" 是锦上添花；"从 0 到有" 是另一个量级。

→ 写回产品时，逐行 OCR 应当是 **`recognize_table()` 返回空时的兜底**，
而不只是"高分辨率时的更优路径"。这与 §34 的 12px 阈值**不冲突、是两件事**：
- 还原成功 → 按 §34 的 12px 阈值选路（质量）
- 还原失败 / 返回 <2 列 → **一律试逐行 OCR**（召回）

### 仍未解决

- 逐行 OCR 在**多面板图**上会把整张图切成一个大网格（`William`/`Baslan`）。
  需要先缩区域，或者加一条"这个网格是不是真表"的判据 —— 与 F-020 是同一个缺口
- 样本仍然很小：7 条，其中真表判定来自 LLM 单次判读、无人核

## 37. 拿 gold 分打一次，**代理指标的结论被推翻**（2026-07-28）

用户问「现在的测试真的帮到策略了吗、还要测什么」。查下来发现**该做的那个测试一直没做**：
§34~§36 量的全是**代理指标**（参照命中、合法码率、非空格子数），
没有一个是 `eval/gold.py --score`。代理指标便宜、能定位错因，
但它回答不了目标(b)那个真问题 ——「瘤系 × 处理 × 响应 三个值在**同一行上**同时对」。

### 37.1 先踩到一个流程洞：**拿过期产物打分**

第一次打分时 `1.output/` 是 **07-25** 的产物，而 B（断行连字符）是 **07-28** 才写回的。
`cyclophos- phamide` 还在里面，于是打分报 `pbc_28772` **漏行 5**。
**那 5 行其实早就修好了，是产物过期。**

根因：**gold 打分没接进 `eval/regression.sh`，`1.output` 改完代码没人重跑。**
重跑 `eval/matrix.sh` 之后：

| gold | 过期产物 | 重跑后 |
|---|---|---|
| `pbc_28772__p03_table_1` | 44 期望 / 计分 39 / 命中 38 / **漏行 5** | 计分 **44** / 命中 43 / **漏行 0** |

**这是 B 第一次被人核 gold 验证**（之前只有单测和 spike 的自比）。

### 37.2 逐行 OCR × CLAHE 的 gold 分：**最优组合是两个一起上**

`spike/probe_rowocr_goldscore.py`。把逐行 OCR 的产物写成同名 CSV 放进 `1.output` 的副本，
再对副本打分（基线不动，可直接对照）。

| 变体 | `pbc_21296 p03_fig_1`（120dpi）<br>计分 / **命中** / 漏行 | `pbc_24724 p05_fig_1`（300dpi）<br>计分 / **命中** / 漏行 |
|---|---|---|
| 基线（整表管线，产品现状） | 5 / **4** / 39 | 0 / **0** / 41 |
| 逐行 OCR | 16 / 6 / 28 | 32 / **29** / 9 |
| **逐行 OCR + CLAHE** | 17 / **13** / 27 | 31 / **29** / 10 |

- **300dpi 那张从"完全打不出分"变成 29 个正确格子**（计分 32 / 命中率 90.6%）。
  原因就是 F-018 —— 锚点是瘤系名，整表管线把首字符吃掉，一行都对不上；
  逐行 OCR 修好首列，行就对上了。
- **120dpi 那张正确格子 4 → 13（3.25 倍）**，漏行 39 → 27。仍然不可用，但不再是 0 附近。

### 37.3 **代理指标给出了错误的建议** —— 这条是本节最该记住的

§34 用「参照命中」（首列标签出现在网格里**任何位置**）得出的结论是：
> 120dpi 上整表 + CLAHE（18/43）优于逐行 OCR（16/43），所以要按 12px/行分流。

**gold 分说反了**：120dpi 上 逐行+CLAHE（命中 13）> 逐行（6）> 整表基线（4）。

差别在于**代理指标不看行**。「标签出现在网格里」≠「标签在正确的行上」。
`log.md` §36 里 `William 2021 p4` 那条 5/5 命中却是假网格，已经预警过一次，
这次是同一个毛病在**决策层**咬人。

**后果：§34 那个「≥12px 走逐行、<12px 走整表 + CLAHE」的分流阈值，不需要了。**
逐行 + CLAHE 在两个分辨率上都是最优或并列最优。
少一个阈值是**净收益** —— 那个阈值余量只有 1.1~2.9 px、单点拟合，本来就是负债。

### 37.4 修正后的写回方案

| 场景 | 做法 |
|---|---|
| 图片表，一律 | **CLAHE → 逐行 OCR**（不再按分辨率分流） |
| `recognize_table()` 返回空或 <2 列 | 同样走上面这条（§36：留出集 108 条还原失败里约 27% 是真表） |
| 原生每行 < 12px | 不改路径，但**降级并在 `notes` 写明像素预算不足**（阈值从"分流"降级为"提示"，脆弱性就不再影响正确性） |

### 37.5 仍未解决 / 效力边界

- `pbc_21296`（8.0 px/行）**仍然不可用**：45 行里只有 13 个正确格子。
  它的身份列在同篇文字表里 43 个标签完好，交叉校验仍是唯一出路
- gold 只有 **5 张表、全在开发集、全是 PDX 论文**。§37.2 的结论**不能外推到留出集**
- 两张图片表都报「锚点不唯一」（21296 有 9 组、24724 有 2 组），
  打分器自己在提示逐格分不完全可信 —— 需要换锚点列，尚未做

## 38. gold 打分接进回归（2026-07-28，用户批准第 1 条）

§37.1 那次事故的直接修法：**打分器不在回归里 ⇒ 没人重跑产物 ⇒ 拿过期产物下结论。**

### 改了什么

| 文件 | 改动 |
|---|---|
| `eval/gold.py` | `--map`（产物目录名映射）、`--baseline`、`--tier`；`cmd_score` 增加基线对账 |
| `eval/expected_gold.csv` | **新增**，逐格准确性的基线快照 |
| `eval/regression.sh` | 加一条 fast 档的 `--prefix gold_21296`（gold 要的文字表快档本来没有）；末尾追加 gold 对账；`RC` 汇总两个断言的退出码 |
| `eval/expected.csv` | 加 `fast,table,gold_21296,p04_table_i.csv,44,cell,high`（新命令的产出） |

**产品代码 `pdf_table_extract/` 一行未改。** 用户明确要求：这属于 response/xenograft 的
特化验证，不进产品（产品不该有领域知识，与非目标一致）。

### 三个设计决定及其理由

1. **`--map` 是必需的，而且它防的是一种静默失败。**
   `regression.sh` 的目录名是 `--prefix`（`gold_28772`），不是 PDF 的 stem。
   打分器默认按 stem 找 ⇒ **全部 miss，而 miss 只表现为少几行输出**，
   不报错、不影响退出码 —— 正是 §37.1 那类"自信的错误结论"。

2. **判定用"变差才红"，不用"必须完全相等"。**
   `expected.csv` 用精确快照是对的（那些是行列数、确定性整数）；
   但 gold 的 `match` 依赖 OCR，模型或 PIL 一动就漂，精确相等会天天亮红灯，
   然后大家对红色脱敏（`AGENTS.md` 里已经有这一课）。
   三档与 `holdout/report.py` 一致：`✓` 一致 / `▲` 变好（提示更新基线，**exit 0**）/
   `✗` 变差（**exit 1**）。

3. **fast/full 分档**：图片表要 OCR，快档 `--no-ocr` 整个不跑图片路径。
   基线里标 `tier=full` 的条目在快档**静默跳过**，不算失败。

### 自测：**断言器必须验证它真的会红**

这条是 `holdout/report.py` 换来的教训（照观测值定验收线 ⇒ 永远 ✓ 通过 ⇒ 等于没装）。
所以故意改基线两个方向各试一次：

| 试法 | 结果 |
|---|---|
| 基线改成 `match 85 / missing 1`（我们本该更好） | `✗ 变差: match 85→84, missing 1→2`，**exit 1** ✅ |
| 基线改成 `match 38 / missing 3`（我们本该更差） | `▲ 变好: match 38→41, missing 3→0`，**exit 0** ✅ |
| 还原基线 | 5/5 `✓`，exit 0 ✅ |

### 验收

```
bash eval/regression.sh        → exit 0，9 张表 + 5 条约束，gold 3/3 ✓
bash eval/regression.sh full   → exit 0，22 张表 + 6 条约束，gold 5/5 ✓
pytest tests/ -q               → 85 passed
```

### 踩坑

**`expected_gold.csv` 第一版把注释写在文件开头**，于是 `csv.DictReader` 把注释行当成表头，
`base` 加载成空字典 —— **基线对账整段不执行、还是 exit 0**，看起来"接好了"。
`expected.csv` 的约定是表头必须第一行；注释一律放表头之后、以 `#` 开头（加载器跳过）。
**又一个"静默失效的断言"**，跟第 1 点是同一类问题，两次都出现在同一个改动里。

## 39. 整行判据 + 锚点唯一性（2026-07-28，用户批准第 2、3 条）

用户明确边界：**这两条只进 `eval/`，不进 `pdf_table_extract/`** ——
它们是 response/xenograft 的特化验证，产品不该有领域知识（与非目标一致）。
本节改的全部是 `eval/gold.py` 与 `tests/test_eval.py`，**产品代码一行未改**。

### 39.1 整行判据（第 3 条）：**逐格命中率一直在掩盖漏行**

新增 `ScoreResult.rows_full / rows_partial / rows_missing / rows_total`，
按 gold 的**每一个数据行**记账，**漏行直接算错**。表头多一列「整行全对」。

一打就现原形：

| gold | 逐格命中率 | **整行全对** |
|---|---|---|
| `pbc_21296 p03_fig_1`（图片表） | **80.0%** | **4/44 = 9%** |
| `pbc_21296 p04_table_i`（文字表） | **100.0%** | 42/44 = 95% |
| `pbc_24724 p05_fig_1`（图片表） | — | 0/41 = 0% |
| `pbc_26870 p18_table_1` | 100.0% | 41/41 = 100% |
| `pbc_28772 p03_table_1` | 97.7% | 43/44 = 98% |

**`p03_fig_1` 报 80% 命中率，实际只有 9% 的行可用。**
逐格率只算"对上号的行"里的格子，漏掉的 39 行根本不进分母 ——
像考试只算你答了的题。而目标(b)要的恰恰是「三个值在同一行上同时对」。

`p04_table_i` 那个 100% 也一样：整行 42/44，差的 2 行就是候选 A 的压行问题。

### 39.2 用整行判据重看逐行 OCR，收益比 §37 report 的更明确

| 变体 | `p03_fig_1`(120dpi) 整行 | `p05_fig_1`(300dpi) 整行 |
|---|---|---|
| 基线（产品现状） | 4/44 = **9%** | 0/41 = **0%** |
| 逐行 OCR | 6/44 = 14% | 29/41 = **71%** |
| **逐行 + CLAHE** | **13/44 = 30%** | **29/41 = 71%** |

注意 `p03_fig_1` 那一列：逐行+CLAHE 的**逐格率 76.5% 比基线的 80% 还低一点**，
但**可用行是 13 vs 4（3.25 倍）**。同一份产物，两个指标给出相反的排序 ——
这就是为什么必须用整行判据做决策。

### 39.3 锚点唯一性（第 2 条）：**主因是全空行，不是列选得不好**

新增 `--suggest-anchors`：给每张 gold 找「在**我们的产物**里也还唯一」的列组合。
只打印列序号与唯一性计数，**不打印任何格子的值**（gold 与产物的都不打）。

先把问题看清 —— 打分器报的"锚点不唯一"发生在**我们抽出来的表**里，
**不是 gold 里**（gold 的唯一性在 `load()` 就当场校验，不唯一根本加载不了）。

实测发现两个原因，第一个是主因：

| 产物 | 数据行 | **其中全空** | `#0` 唯一性 | 去空行 + `#0+#1` |
|---|---|---|---|---|
| `p03_fig_1` 基线 | 44 | 0 | 43/44 | **44/44 ✔** |
| `p05_fig_1` 基线 | 46 | **8** | 29/46 | **38/38 ✔** |
| `p03_fig_1` 逐行+CLAHE | 46 | 1 | 36/46 | 41/45（`#0+#1+#2` → 44/45） |
| `p05_fig_1` 逐行+CLAHE | 46 | **4** | 40/46 | 41/42（`#0+#1+#2` → **42/42 ✔**） |

**修法一（已做，安全）**：`split_rows` 丢掉整行全空的行。
理由不是"为了让数字好看"，而是它**不可能对上 gold 里任何一行**，留着只有两个坏处：
锚点变空串 → 多个空行互相撞车 → 整张表的逐格分被标成不可信；以及虚增"多出行"。
这**不会掩盖"产品吐空行"** —— 行数由 `expected.csv` 按 manifest 的 `n_rows` 断言，那份计原始行数。

**修法二（未做，需要用户决定）**：把两张图片表的 `anchor_cols` 从 `#0` 改成 `#0 + #1`
（逐行 OCR 那条路要 `#0 + #1 + #2`）。**没自己动手的原因**：改锚点定义就要重写 gold 正文里
每一行的 `anchor` 键（`BT-29` → `BT-29|<组织学>`），而组织学列不在 `check_cols` 里、
gold 正文可能根本没有它 —— 得重新生成。那是用户的答案文件，不该我替他改。

### 39.4 单测：换了触发条件，同时补了一条

`test_duplicate_anchor_is_finding_not_crash` 原来用"末尾几行全空 → 锚点全是空串"造重复，
而全空行现在在源头被丢掉了，测试于是失败。
**它的意图（重复锚点不许崩）仍然有效**，改用**真正重复的值** ——
OCR 把两个不同瘤系名读成同一串，实测 `p03_fig_1` 上 `ALL-17` 出现 4 次。
另补 `test_all_blank_rows_are_dropped`。

写这条新测试时断言写太宽（`assert not res.shape`），报的却是"列数 期望 3 实际 2"
—— 测试辅助函数的默认列数，跟我要验的行数无关。**断言要对准被测的那一维。**

### 39.5 验收

```
pytest tests/ -q               → 86 passed
bash eval/regression.sh        → exit 0，gold 3/3 ✓
bash eval/regression.sh full   → exit 0，gold 5/5 ✓（含新增的「整行全对」列）
```

`expected_gold.csv` 增加 `rows_full` / `rows_total` 两列纳入基线保护。
`rows_total` 变了单独报 ✗ —— 那是 gold 标注量改了，此时 `rows_full` 的比较没有意义。

## 40. 候选 A 写回产品：**只检测不拆分**（2026-07-28，用户批准）

`log.md` §30/§32/§33 里 A 被拦过两次（留出集 14/14 误报、阈值验证集 17 拆 2 对）。
这次找到了两个真正的根因，两个都不是"阈值没调好"。

### 40.1 根因一：表头排除写死成"只排第 0 行"

`tall_rows` 用 `r0 < 1` 跳表头，等于假设表头就一行。
而多级表头 docling 会拼平成一格（PDF 事实 #16），行高天然偏高 → 整行误报。
阈值验证集 22 处检测里 **10 处在行 1、3 处在行 2**，逐条看全是这个。

**写死跳过前 N 行也不行**，实测表头行数 1/2/3 都有：

| 表头行数 | 实测 |
|---|---|
| 1 | 多数表 |
| 2 | `Hiyama_1995`、`Lucchesi_1968` |
| **3** | `Ng 2017`、`Nowell 1976`、`Knudson_1971` |

跳 2 行 → 3 行表头照旧误报；而 1 行表头的表里第 1 行是**真数据**
（`Hiyama p4 行7`、`Dobzhansky 行34` 都是真压行），跳掉就漏。

**解法：用 `docling_core.TableCell.column_header`** —— docling 自己就标了。
它给的答案与人眼一致（`Hiyama`/`Lucchesi` = `[0,1]`，`Ng`/`Nowell`/`Knudson` = `[0,1,2]`）。
**这是布尔标记不是数值，所以没有余量、不用重验、换语料不会漂** —— 比再造一个阈值好。

### 40.2 根因二：两套视图的行号不通用（这条是 A 一直"拆错行"的真凶）

产品写进 CSV 的网格来自 `export_to_dataframe()`；压行检测需要格子高度，
只有 `table_cells` 有。**这两套视图的行号对不上**：

| | |
|---|---|
| 阈值验证集 427 张表 | 行数一致 243 张 / **不一致 184 张（43%）** |
| 偏移量 | 0 或 -1（= 表头行数 - 1，多级表头被 dataframe 拼平） |

后果：在格子的第 41 行发现问题，去 `rows[41]` 报告 —— 实际指向第 40 行，**报到无辜的行上**。
早先看到的 `原格: []`、以及"原格与拆出内容完全不同"，全是这一个原因。

**解法：`match_cell_row_to_output_row` 按内容配对，不按行号。**
实测 6/6 命中，重叠率**全部 100%**（8/8、7/7、6/6、7/7、8/8、7/7），与次优差距悬殊，
所以不需要阈值。

### 40.3 90 篇实测精度（开发集 12 + 留出集 25 + 阈值验证集 53）

| 判据 | 检测处数 | 人核 |
|---|---|---|
| 1+2 层（表头只排第 0 行） | 23 | 约 12% 正确 |
| + 第 3 层 `column_header` | 14 | **9 真 / 4 误 / 1 存疑 ≈ 64%** |
| + 第 4 层拆分确认 | 10 | **9 真 / 1 误 ≈ 90%** |

**残留的 4 个误报全是同一机制：长标签折行** ——
`Image size (pixels)`、`Pen Digits (1797x64)`、`|Corr (BMI)| ↓`、
以及 `96.21 | 96.02 (96.10 | 95.70)`（值 + 括号补充写成两行）。
其中 3 个被第 4 层挡掉；挡不住的 `de_Bruijne p603` 三列都这么折行，几何上与真压行无法区分。
**误报是集中的**：4 个里 3 个来自同一篇会议论文集（`de_Bruijne`，上千页）。

### 40.4 一个必须记下的选样偏差（我自己犯的）

先在 7 篇上试，报出"精度 54%→100%"。**那 7 篇是我按"修之前报过误报"挑的** ——
正好就是这个修法专治的类型，100% 几乎是注定的。
90 篇全量才是诚实的数字（64% / 90%）。
**凡是从"出过问题的样本"里挑子集做验证，结论必然偏乐观。**

### 40.5 为什么只检测、不拆分

自动拆分实测**静默丢字符**：

| 表 | 原值 | 拆后 |
|---|---|---|
| `Rovillain p277` | `56` | `6` + 空 |
| `pbc_21296 行41` | `> EP > EP` | `>EP` + 空 |

丢掉之后 CSV 里看不出任何异常 —— 正是铁律 #1 要防的"网格被信任高于原文"。
而只报告的失败方向是"多挂一张黄牌"，代价是多看一眼。

有了 40.1 + 40.2，黄牌能**指名到行并附原文**：

```
merged_row?=行41: ALL-8 ALL-16 | ALLT-cell ALLT-cell | > EP > EP | < 0.001 < 0.001
```

### 40.6 改动清单与验收

| 文件 | 改动 |
|---|---|
| `models.py` | 新增 `Word`、`TableCellBox` |
| `pdfio.py` | 新增 `page_words()`（**必须做旋转变换**，否则横向表一个词都框不到） |
| `engine_docling.py` | `DoclingTable` 增加 `cells` 字段，`convert()` 填充（含 `column_header`） |
| `rules.py` | 新增 `suspicious_tall_rows` / `confirm_merged_row` / `match_cell_row_to_output_row` / `detect_merged_rows`；`decide_confidence` 见到 `merged_row?=` 降到 medium |
| `__main__.py` | `run_text_path` 里调用（**懒算词坐标**：先跑不要 PDF 的前三层） |
| `tests/test_rules.py` | +10 条纯逻辑单测（表头标记、均匀度闸门、rowspan、折行 vs 压行、按内容配对） |
| `eval/expected.csv` | `pbc_21296 p04_table_i` 的 confidence `high`→`medium`（2 处，有意重新基线） |

```
pytest tests/ -q               → 96 passed（+10）
bash eval/regression.sh        → exit 0，9 张表 + 5 条约束，gold 3/3 ✓
bash eval/regression.sh full   → exit 0，22 张表 + 6 条约束，gold 5/5 ✓
```

**重新基线的两处都能追到 A，没有"原因不明"的变化。**
gold 分 5/5 完全不变 —— 符合预期，因为只告警不改数据。
开发集 12 篇全量跑下来只报了 1 处告警，就是已知的那一处。

### 40.7 未做 / 已知边界

- **不做自动拆分**。若将来要做，前提是先实现「拆后字符总数自检」（`split_lossy`）并验证
- `HEIGHT_EVEN_MAX = 0.30` **是在留出集上量的**，留出集对它不再是独立证据
- 长标签折行那 1 类残留误报（`de_Bruijne p603`）几何上分不开，需要新判据
- 真阳性样本仍然偏少：90 篇里 9 处，其中开发集只有 1 处

## 41. 第三批语料：PPTP 97 篇泛化跑，**第一次在没参与开发的文献上真跑抽取**（2026-07-30）

用户要求：从 `01_paper_extract/data/collections/` 里找 10 篇没在 gold 集里的文献，
跑 `response` 与 `xenograft` 两组关键词。

### 41.1 这批料补上了什么、补不上什么

`collections/` 下只有 `pptp/` 可用（105 篇；其余 4 个 collection 合计 1 篇 PDF）。
**105 篇里 8 篇与开发集重合，且逐字节 md5 相同**（`gen/stage.py` 的自检之一），剩 97 篇全新。

- **补上的**：`holdout/` 刻意只跑 `--list`，所以"抽取质量在新文献上对不对"一直没人验过。这次验了。
- **补上的**：`NEXT.md` §5 的第一缺料「更多横向转正表」—— 开发集 3 张、留出集 1 张（还只有 3 个数据行），
  这批一次给出 **22 页 / 16 篇**。
- **补不上的**：97 篇全属 PPTP 同一领域，**只验"出版社/年代/版式"轴**。领域轴仍然只有 `holdout/`。
  从这批得出"它泛化了"就是重犯「分母不对，比例就是假的」。这句要跟着结论走。

新增 `gen/`（stage / run / make_prereg / check + README 的边界声明），产物在**仓库外**
`~/Downloads/pte_pptp_gen/`。**理由变了**：`AGENTS.md:477` 写的「本项目没有 git」**已经过期**
（现在有 2 个 commit），误提交一次就永久进历史，所以不再走"加一行 gitignore"，直接放仓库外。
**这句过期的话要改** —— 它同时是 `eval/regression.sh` 存在理由的论据。

### 41.2 方法上加了预登记，而且它当场救了两次

`gen/prereg.csv` 在跑抽取**之前**写死（md5 `7600686614fc459a…`）：78 条，构成断言的 48 条，
判不了的 30 条标 `?`。**`?` 不算断言** —— 本批结论强度的上限就是那 48 条。

**踩坑一：草稿判据会高估命中。** 第一版想用"caption 行到下一个标号行之间的文本块"自动判，
实测 `pbc_26825 TABLE 1`（体外 IC50 表）的 173 行区块里检出了 `response` —— 来自同页正文。
照这个写死预登记，跑完会把工具的**正确行为判成误报**，比没有预登记更糟。改成三档规则：
caption 命中（工具自己的 matcher）⇒ 确定 hit；**整页文字层无该词 ⇒ 确定 miss**；
页里有词且出现只可能是表头的串 ⇒ hit/cell；其余一律 `?`。

**踩坑二：我自己的预登记错了 2 条，预登记把它暴露出来了。** `ccr_18_2675` 的
Table 3/4/5 同在 p6，我用页级探针把 Table 3 的表头串 `Responder` 记到了 Table 4/5 头上。
人读 p6 渲染图确认：Table 4 是 `Gene | RsSNP ID | Description | Genotype`、
Table 5 是 `AURKA genotype | Cycle 1 DLT | Grade 2+ toxicity`，**都不含 `response`，
工具不出它们是对的**。⇒ 报出的 6 条"漏抽"里 **2 条是我的错、4 条是真的**。
没有预登记的话，这 2 条会被写成工具缺陷。

**诚实分档**：预登记是 **agent 读 PDF** 得出的，按 `eval/gold.py` 的分档只能算 `agreed`，不是 `human`。

### 41.3 阳性对照（这一步不能省）

`posctl_pbc_29304` = 开发集成员，正确值钉在 `eval/expected.csv`，**走完全相同的摊平路径**，
排在 12 篇的最前面。结果 `42x15 / matched_on=caption / high / spans_pages=3-4(caption_continued)`，
与 `expected.csv:19` 逐字相同 ⇒ symlink、前缀映射、conda 环境、`-o` 语义全部无误。
理由是 §35.1 那次：macOS 上没有 `timeout`，25 个输出目录全空，而"这批料没有图片表"
这个结论**看起来完全合理**。任何"0 命中"都必须有同批阳性对照垫底。

### 41.4 跑出来的数

36 条命令**全部 exit 0**。manifest 87 行 = 出表 28 行 + 失败 59 行，唯一 CSV 20 张。
`confidence` high 15 / medium 10 / low 3；`matched_on` **cell 16 > caption 12**
—— 再次印证"文字表必须把表内容一起匹配"那个修正过的决定：一半以上的命中靠的是表内容。

| 断言结果 | 数 |
|---|---|
| 真漏抽 | **4**（全在 `j_pharmthera_2024_108742`，见 F-022） |
| 我预登记错 | 2 |
| **误报** | **0** |
| 形状不符 | `pbc_22188` 13 列 vs 人核 12 列（F-023）；confidence medium vs 我写的 high（**工具是对的**，它因 `merged_row?` 正确降了级，是我预登记写错） |
| 退化 CSV | 1（F-019 复现） |
| high 且 notes 空 | 10 行 / 去重后 8 张，其中 `eval/checks.py detect` 判定**真有问题的 1 张**（F-024） |

### 41.5 三条新 finding（详见 `eval/findings.md`）

- **F-022 转正页的 caption 描述段抓不到 → 静默漏 4 张表。** 机制定位到
  `pdfio._absorb_legend_tail()`：它要求续段 block 与 caption block **横向重叠**，
  而横向表页上"视觉的上下相邻"在页坐标里是**左右并排**（实测 p61 重叠 = **-1pt**）。
  那句 `continue` 本是为跳过 PMC 竖排水印加的，现在连真 legend 一起跳。
  后果最坏的一点是**工具没举手**：caption 没命中 ⇒ 铁律 #2 的失败行不会留 ⇒ manifest 里什么都没有。
- **F-023 页码与书眉被吸成表的第 13 列**（表头 `'308'`、首格 `'Smith'`），`notes` 无提示。
- **F-024 `(Cont'd )` 没拼上，但根因是首页两层表头解析崩了**，`same_column_names` 拿崩掉的
  表头去比正确的表头。**判据设计本身没错** —— 同批的 `Table 2 continued`（无括号）
  靠判据二正确拼上了（`spans_pages=6-7(same_columns)`）。

### 41.6 被这批数据推翻/修正的既有判断

1. **`VERTICAL_LINES_THRESHOLD=30` 的下侧余量从 2.7x 缩到 1.33x。**
   22 个转正页竖排行数 **40–545**。此前文档记的真横向表下界是 **82**（`Bruhm 2025` p9）。
   新下界 **40**（`pharmthera` p63），**余量只剩 1.33 倍**。
   6 个最低值（40/45/59/60/95/121）**全部人核确认是真横向表**（渲染原始 PDF：表是躺着的、
   书眉正立、`page.rotation==0`，符合事实 #3）—— **不是误报，是真信号**。
   低是因为**表小**（Table 2 只有 7 数据行 × 6 列、整页大片空白），不是信号弱：这 6 页的
   **占比高达 93–97.6%**。所以「绝对行数 AND 占比」这个组合设计是对的，
   只是绝对行数那一侧的余量比原先以为的窄得多。
   ⚠ **6 个低值全来自同一篇**，低侧的分母是 **1 篇**。不要按 n=97 引用这个余量。
2. **`(Cont'd )` 与无括号 `continued` 是两种不同的结局**，不能一起说成"新写法不支持"：
   前者没拼上（但根因在表头），后者拼上了。
3. **U+2013 短破折号会命中，ASCII 连字符不会。** 匹配器是
   `(?<![\w-]){body}(?![\w-])`（`rules.py:131`），lookaround 只排除 `\w` 和 ASCII `-`。
   实测 `pbc_22921 TABLE III` 的 `Dose–Response` 用的是 **U+2013**，
   `-k response` **靠 caption 正确命中**；而 `dose-response`（ASCII）不会。
   ⇒ 给已知限制 #5 补一条：**语料里实际用的是短破折号，那个担心的漏在这批料上不会发生**。
4. **F-003（标号漏检，"最大的盲区"）在 97 篇上零真漏检。** 非循环探针只报 2 处疑似，
   逐条读上下文全是正文引用。这是该判据迄今最大的一次检验。

### 41.7 还没做的（别当成做过了）

- **OCR 档（`out_full/`）没跑。** 本轮全程 `--no-ocr`。原因：10 篇里有 38 个图 legend，
  实测约 25 个命中两组关键词之一，而**未命中 legend 的候选还要付一次全量 OCR 才能测关键词**
  （`__main__.py:434-448`），成本 30–120 分钟且形状不可预测；且 OCR 产物无条件 `low`、
  本来就要人核。`--no-ocr` 已免费给出图片路径的普查（59 条失败行）。
  **两档必须写到不同输出根目录** —— `emit.describe_query()` 不看 `--no-ocr`，
  共用会让后跑的静默替换先跑的 manifest 行，而先跑的 CSV 还留在盘上像命中。
- **20 张 CSV 只人核了 4 张**（`pbc_22188` 逐行、`ccr_18_2675 Table 3`、
  `can_16_0122 Table 1` 两页、`blood` 退化那张）。其余靠 `eval/checks.py detect` 的零标注判据。
- **22 个转正页只人核了 6 个**（`pharmthera` 那 6 个低值）。另 16 个只有阈值观测、没看图。
- `j_celrep_2019_09_071` 是开发集 preprint `566455` 的**正式发表版**（标题逐字相同），
  **不计入 10 篇**，只当配对对照。
- 顺带发现的文档小错：开发集文件名 `blood_2014_12_518900.pdf` 的 `518900` 是笔误，
  该篇 DOI 实为 `10.1182/blood-2014-12-618900`。**只是文件名，不要改**
  （`eval/expected.csv` 等多处引用）。
