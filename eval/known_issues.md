# 已知错误行为（`eval/expected.csv` 里被当作"现状"钉住的那些）

`eval/expected.csv` 是**现状快照**，断言的是「行为没变」，不是「行为正确」。
下面这些条目是**已知错误的当前行为** —— 它们在 expected.csv 里以错误的值被钉着，
这样断言器才能全绿、才能起到"改代码别改坏"的作用。

**下一轮统一修的时候，一条条从这里挪出去，同时改 `expected.csv` 里对应的数字。**

完整证据见 `eval/findings.md`。

| expected.csv 里的条目 | 钉着的（错误）值 | 应该是 | 对应 finding |
|---|---|---|---|
| `resp_21296/p04_table_i.csv` | `44` 行 | **`45`**（44 个数据行 + 表头）—— 第 41 行把 `ALL-8` 和 `ALL-16` 两个数据行压成了一格 | F-001 |
| `resp_24724/p05_fig_1.csv` | `47` 行 | **`45`**（44 个数据行 + 表头，`AGENTS.md` 事实 #9 的人眼核对值）—— 末尾 8 行全空、表头缺首列名 `Xenograft Line` 导致整体左移 | F-005 |

## 不在 expected.csv 里、但同样已知的问题

这些不影响端到端产物的形状，所以钉不进 expected.csv：

| 问题 | 说明 | 对应 finding |
|---|---|---|
| ~~转正判据在留出集上误报~~ | **✅ 2026-07-26 已修** —— 加了第三条判据，三语料 9 真表全中、0 误报 | F-002 |
| 留出集 17/25 篇检出 0 张表 | 分不清是真没有（supplementary）还是 caption 漏检。**需要人工抽 2–3 篇核对** | F-003 |

## 与 `AGENTS.md` 的关系

`AGENTS.md` 里那张 42 行的手抄基线表**已经删掉了** —— 它跟本目录的 `expected.csv` 是同一批
数字的两个副本，而且**漂了 2 处**（`resp_24724/p05_fig_1` 文档记 45、实际 47；
`resp_30017` 文档记 `matched_on=cell`、实际 `caption`），很久没人发现。

两份手维护的同一批数字必然漂移。**现在只有 `expected.csv` 一份**，`AGENTS.md` 只留规则。
这两处漂移本身就是 `eval/checks.py regression` 存在的理由。
