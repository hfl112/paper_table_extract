# Synonyms for treatment-response tables; one word per line, OR semantics, # = comment.
# Matching is whole-word + English inflections (responses/responded match automatically).

response
relapse
remission

# Response codes (PD/CR/PR/SD/MCR) are deliberately absent: too common in prose and
# SD doubles as standard deviation. Filter downstream on the extracted CSVs instead.
