# pdf-table-extract

给一篇**英文**文献 PDF 和一组关键词，把命中关键词的表格（含以图片形式存在的表格）导出成 CSV。

一个可移植的单一用途 CLI。**不是**流水线的一环，**不依赖**任何上游产物或兄弟目录。

> **技术路线不写在这里。** 引擎选型、定位机制、模块划分都在当前 plan 文件里，允许随实测变更 —— 开发中它已经被真实数据推翻过三次。这份文件只放"一次实验推翻不了"的东西。

## 三份文档的分工（**每次干完活都要更新 `log.md`**）

| 文件 | 放什么 | 寿命 |
|---|---|---|
| **`AGENTS.md`**（本文件） | 只放"一次实验推翻不了"的：目标/非目标、开发环境、**PDF 事实**、CLI 与输出契约、铁律、测试料基线、已知限制 | 长期 |
| **`log.md`** | **过程与判断的历史**：做了什么、测了什么、结果如何、**踩了什么坑、为什么改主意**、阈值余量的诚实分档、事故与恢复 | 长期，只追加 |
| **`NEXT.md`** | 后续计划：下一轮要修什么、gold 标注格式、评价框架、不做的事 | 中期，做完就并进 `log.md` |
| **`eval/findings.md`** | **问题清单**（`F-00x` 编号）：证据、开发集/留出集各命中几次、工具举手了没、修法假设 | 修完一条删一条 |
| **`gen/README.md`** | 第三批语料（PPTP 97 篇）的**边界声明**：它能证明什么、不能证明什么。放这儿是为了防止后人拿同领域结果当跨领域证据 | 长期 |
| plan 文件（`~/.claude/plans/`） | 当前技术路线、待办、开工前必验项 | 会变、允许作废 |

**三批语料各管一个轴，不要混用**（详见 `gen/README.md`）：
`0.pdf_input/` 开发集 12 篇（阈值就是在它上面拟合的，**不是证据**）／
`holdout/` 25 篇**只跑 `--list`**，验**领域轴**／
`gen/` PPTP 97 篇**跑抽取**，验**出版社·年代·版式轴**与抽取质量本身。

### 更新 `log.md` 的约定

**每完成一段工作（修完 bug / 跑完一轮测试 / 改了某个判据）就追加进 `log.md`**，不要等到最后补。要写进去的是：

1. **做了什么改动**，以及**它推翻了之前哪个判断**（这条最值钱 —— 本项目的路线被推翻过三次，每次都是数据纠正了推理）
2. **测了什么、结果的具体数字**（不要写"效果变好了"，要写 `45×7` / `PR 从 130 降到 8` / `覆盖率 99% → 50%`）
3. **踩坑**：错误的判断连同**当时为什么那样想**。只记结论、不记错误路径，后人会重犯
4. **阈值**：新增或调整的阈值，连同两侧观测值和余量倍数；**单点拟合的要明说**
5. **未决/已否决**的事项，以及**否决理由属于"选择性不够"还是"技术做不到"** —— 这两者混淆会让后人在错误前提下重开讨论

反面例子（不要这样写）：「优化了图片表抽取，效果提升」。
正面例子：「图片候选来源从"嵌入位图"改成"图区域"—— 因为 `pbc_21078` Fig.3 是矢量热图、该页位图数为 0，两条路都看不见它，**静默漏表**（用户人眼核对才发现）。改后 Fig.3/Fig.4 都抓到。」

## 明确的非目标

这些是刻意不做的，不要"顺手加上"：

- **不做 LLM 语义抽取**。只负责"表格 → 忠实的 CSV"，不负责"表格 → 结构化领域记录"。任何把单元格解读成受控词表（如 `objective_response ∈ {CR,PR,SD}`）的需求都属于下游。
- **不引入领域 schema**。输出 CSV 的列就是原表的列，不重命名、不做单位换算、不做实体标准化。
- **不做多阶段留痕**（ledger / roster / gold / 门禁 / provenance 指纹）。这是前身项目的过度设计。
- **不联网、不调 API**。纯本地。
  **例外（2026-07-26 用户批准）**：这条约束管的是 `pdf_table_extract/` **本体**，它至今一行网络
  代码都没有。**`eval/` 下的评测脚手架允许调 LLM**（`eval/llm.py` 用 Claude 当独立第二读者，
  找零标注判据看不见的错）。评测不随包发布、不影响 CLI 的纯本地属性。
  看到 repo 里有 API 调用**不代表这条非目标作废了** —— 先看它在哪个目录。
- **不处理中文文献**。

## 开发环境

开发期一律用现成的 conda 环境 **`gemini`**（Python 3.11.15），不要新建 venv、不要 `uv sync`、不要 `pip install`：

```bash
conda run -n gemini python -m pdf_table_extract <args>
```

已验证可用（不要重复检查）：

| 库 | 版本 | 备注 |
|---|---|---|
| pdfplumber | 0.11.10 | `Page.find_tables/crop/within_bbox`、`Table.bbox/extract` |
| docling | 2.110.0 | `do_table_structure` / `do_ocr` / `TableFormerMode.ACCURATE`；`TableItem.export_to_dataframe/caption_text/get_image` |
| paddleocr | 3.7.0 | 纯文字 OCR（`PaddleOCR(lang="en")`）、`PPStructureV3` |
| paddlex[ocr] | 已装 | `PPStructureV3` 的必需依赖 —— **只 import 成功不代表能用**，早先漏了这个导致 `predict()` 报 `DependencyError`。装它只新增 7 个小包（cachetools / cssutils / encutils / ftfy / more-itertools / premailer / tiktoken），未改动任何现有包 |
| paddlepaddle | 3.3.1 | CPU（Apple Silicon 无 GPU 版） |
| PyMuPDF | 1.27.2.3 | `Page.get_image_info/search_for/get_text/get_pixmap/find_tables` |
| pandas | 3.0.2 | 写 CSV |

docling 的 OCR 后端 `rapidocr` 已装，`tesseract` 二进制在 `/opt/homebrew/bin/tesseract`。

**`gemini` 是共享环境**，装错会影响其他项目。需要环境里没有的库时先问，不要自己装。

## PDF 事实

这些是文献 PDF **本身**的性质 —— 换引擎、换路线都不会变，且每一条不知道就会写出 bug。全部由 `0.pdf_input/` 里的 7 篇真实文献实测得出。

1. **caption 正则会被换行截断。** 实测同一个标号出现 `Supplementary\nTable S2` 和 `Supplementary Table\nS2` 两种断法，会被当成两个不同标号 → **匹配前必须先折叠空白**。

2. **区分"真 caption"与"正文引用"的判据：标号位于某一「行」的开头，且标号后紧跟 `.`、`:`、空格、或行尾。**
   `^(?:Supplementary\s+)?(?:Table|TABLE|Tab\.)\s+(?:S?\d+|[IVXivx]{1,4})(?:[.:\s]|$)`
   **6/6 篇与 docling 实际抽到的表完全吻合。** 不依赖全大写（那只是 Pediatr Blood Cancer 的惯例，不通用）。
   两个坑，都是踩过的：
   - **不能用「block 开头」**：PyMuPDF 会把相邻的两个 caption 合并成一个 block。实测 blood p6 把 `Figure 4.` 的 caption 和 `Table 3.` 的 caption 并成一个 block，`Table 3` 在 block 内偏移 **931** → 用 block 判据会漏掉整张 Table 3。而它在该 block 的第 12 **行**行首。
   - **标号后必须允许行尾**：实测 pbc_28772 / pbc_29304 的 caption 行**只有 `TABLE 1` 三个字**，描述文字在下一行。要求标号后必须跟 `.:` 或空格会漏掉它们。

2b. **标号只被引用、没有真 caption ⇒ 该实体不在这个 PDF 里。** 实测 CCR-18-2728 引用了 `Supplementary Table S1–S6`、`Table S2–S4` 共 8 个标号，但真 caption 只有 `Table 1` —— supplementary 是单独的文件。同理 pbc_28772 引用了 `Table 2/S1/S2`、pbc_21078 引用了 `Table IV/V/VI`、blood 引用了 `Table 4`，都不在 PDF 里。
   → 若这类标号的引用上下文命中了关键词，必须报 `entity_not_in_pdf` **提示用户去单独下载 supplementary**，而不是当作"没有匹配的表"。

2c. **判"横向表"需要**三个**条件 —— 少一个都会错。**（2026-07-26 从两个加到三个）

    **第三条：竖排文字里必须含 `Table N` 标号，或上一页已判为横向表。**
    加它是因为**前两条组合起来也不够了** —— 78 篇实测（留出集 + 阈值验证集）：

    | 页 | 竖排行 | 占比 | 真相 |
    |---|---|---|---|
    | `Li et al. 2025` p6 | **1555** | 85% | 多面板图 Y 轴标签 |
    | `Williams(cells of)` p15 | 90 | 67% | 染色体臂标签 |
    | `5D5JKFJM` p17 / `5PMEJG7T` p6,p9 | 71 | 51–64% | **`©2017...` 版权声明被逐字符拆成 71 个"竖排行"** |

    它们的占比（51–85%）**全落在真横向表的区间（57–99%）里**，前两条分不开。
    而横向表的 caption 是跟着表一起转过来的，所以竖排里必有标号 —— 实测 9 页真表全有、
    6 页误报全无。
    「或上一页已转正」是**必需的兜底**：`pbc_26870` p19 是真横向表但**caption 为空**
    （事实 #12 的样本），竖排里没标号，单靠标号判据会误杀它。

    **下界又被拉低了一次：2026-07-30 PPTP 语料实测到 40 行，`VERTICAL_LINES_THRESHOLD=30`
    的余量只剩 1.33 倍。** 22 个转正页竖排行数 40–545，最低的 6 个
    （40 / 45 / 59 / 60 / 95 / 121）**全部人核确认是真横向表**（渲染原始 PDF 看：表躺着、
    书眉正立、`page.rotation==0`）。低不是因为信号弱，是因为**表小** ——
    这 6 页的竖排占比高达 **93–97.6%**，是全部 22 页里最高的一档。
    ⇒ 「绝对行数 AND 占比」这个组合本身是对的，但**绝对行数那一侧已经很紧**。
    ⚠ 这 6 个低值**全来自同一篇**（`j_pharmthera_2024_108742`，68 页综述），
    低侧的分母是 **1 篇**，不要按 n=97 引用这个余量。

    下面是前两条的三档实测数据（真横向表的下界先被 `Bruhm 2025` p9 从 134 拉到 **82 行 /
    占比 57%** —— 半页表半页正文）：
    三档实测数据（前两次开发只见过第一、三档，所以只用了绝对行数，结果误判）：
    | 情况 | 竖排行数 | 竖排占比 | 谁能挡住 |
    |---|---|---|---|
    | Wiley 侧边下载水印 | **1** | — | 绝对行数 |
    | **多面板图的 Y 轴标签**（Becker 2022 p4/p6，留出集） | **41–79** | **17–25%** | **只有占比**（`UMAP dimension 2` / `log2FC (36,374 peaks)` / `PC2 (7.0%)` —— 单细胞论文一页几十个小图，每个都有竖排 Y 轴标签） |
    | 真横向表 | 134–446 | 69–99% | 两个都过 |
    | 图片页的水印（pbc_21078 p7） | 1 | **12.5%** | **只有绝对行数**（该页水平文字仅 7 行） |

2d. **判"出版社 OCR 文字层"看**整篇**，不看单页。**（2026-07-26 改，90 篇实测推翻原判据）

    **判据：整篇有 >=80% 的页被整页位图覆盖，且整篇平均 >=1000 字/页。**

    原判据（整页位图 且 **该页** >=1000 字）**两类错同时存在，而且顺序是反的**：
    真扫描页 `Dobzhansky_1946 p12`=602 / `Rous_1911 p21`=681 / `Bell_1876 p3`=832 **漏判**；
    现代大图页 `Chunduri_2022 p7`=1008 / `Drapkin_2018 p1`=1122 **误报**。
    真扫描页的字比误报页**更少**，所以调高加重漏判、调低加重误报 —— **字数分不开这两类**。
    真实余量只剩 **934 ← 1000 → 1008**，74 个字符。

    换成整篇维度就完美分离：

    | 类别 | 篇数 | 整页位图页占比 |
    |---|---|---|
    | **真扫描件**（1876–2005） | **21** | **97%–100%** |
    | 现代论文含整版大图 | 5 | **6%–21%**（含下面那个 Beddows） |

    **余量 76 个百分点，零误报零漏判。** 平均字数那条降为副判据，用来挡"整本扫描但根本
    没做 OCR"（那种得走 OCR 路径，误判会让它跳过图片候选、整篇丢光）——
    真扫描件实测 1128–9668 字/页，**但负例侧无样本**。

    以下是原来那条「单页字数」判据的实测依据，保留供回查： 只看整页位图会把**整版大图页**误判成扫描件 —— 而代码对这类页**跳过图片候选**，后果是整版大图里的表**静默漏掉**。
    实测：真扫描件文字层 2163–6398 字/页（EAP gastric 5255–6398、rstl.1665 2163–2462）；而 Beddows 2023 p24–29（留出集）是全页 supplementary figure，整页位图 100%×100% 但文字层**仅 185 字/页**（只是页眉）。阈值 1000 两侧各有 2–5 倍余量。

2e. **判 preprint 要**两条并列判据** —— 两类预印本的水印形态根本不同。**（2026-07-26 加第二条）

    | 平台 / 模板 | 水印形态 | 哪条判据管 |
    |---|---|---|
    | bioRxiv / medRxiv | **每页页脚都盖** | ① 页数占比 |
    | arXiv + `arxiv.sty` 类模板 | 每页页眉 `A PREPRINT` | ① 页数占比（碰巧） |
    | **arXiv + 期刊/自定义模板** | **只盖第 1 页左边缘**，竖排 | ② **结构判据** |

    **② 第 1 页最左 8% 内、竖排、含「平台名 + 编号」**（`arXiv:2607.00042v1`）。
    实测 4 篇 arXiv 的页数占比：`Perrone 2020` 90%（①抓到）、`Li_2013` 33%、
    `McInnes_2020` 10%、天文那篇 **0%** —— **①只抓到 1/4**。
    那篇天文论文 8/27 页含 `arXiv`，其中 **1 页真水印、7 页是参考文献引用**，
    噪声是信号的 7 倍 —— **把 `arXiv` 加进词表只会更糟**（就是下面 Baslan 那个坑的重演）。
    结构判据实测 90 篇**零误伤**、4 篇 arXiv 全中，检出率 1/4 → 4/4。

    以下是①的实测依据： 真 preprint 的特征是页脚水印盖**每一页**。
    实测：`566455` 54/64 页 = **84%**；`Baslan 2022`（留出集，正式发表版）2/33 页 = **6%** —— 它的 5 次命中全在**正式版的标准栏目和参考文献**里：Nature 的 `peer review information; details of author contributions`、以及一条引用别人 preprint 的参考文献 `Preprint at bioRxiv https://doi.org/10.1101/...`。用总次数判会撞阈值误报。

> **⚠️ 2c/2d/2e 的证据基础只有 5 篇留出集论文**（`Zotero_Precancer_PDFs` 的前 5 篇）。它们各自补上了"我原先没见过的中间档"，但**远不足以说明阈值已普适**。扩大留出集是下一步的第一优先，见 `log.md` §8/§11。

3. **横向表是旋转文字，不是旋转页面。** `page.rotation` 恒为 0，靠文字行方向 `dir` 判断。实测竖排行数：真横向表 pbc_21078 p4=**352**、p5=**134**，pbc_29304 p3=**198**、p4=**264**；其余每页恒为 **1** 行（Wiley 侧边下载水印）。中间隔两个数量级，阈值取"竖排行数 ≥ 30"极安全。
   **必须用绝对行数，不能用占比**：pbc_21078 p7 是图片页、水平文字仅 7 行，那 1 行水印的占比就冲到 12.5%，而真横向表 p5 的占比是 68.7% —— 占比判据会把 p7 误判成横向表。

4. **必须抠原生分辨率嵌入图，不要渲染整页。** pbc_24724 p7 的图原生 2004×2056、显示 481pt 宽 → 有效 300dpi；按 200dpi 渲染整页只得 1336px，**白丢 33% 线性分辨率**。反过来 pbc_21078 的图只有 ~120dpi（660×998 显示 396pt），提 dpi 也救不回来。

5. **确定性表格「发现器」在真实文献上失效 —— 注意这只否定了「发现」这个角色，不否定 pdfplumber 本身。**
   测的是"让它自己在整页上找出表格在哪"（`find_tables()`）：pdfplumber lattice 与 PyMuPDF `find_tables()` 在 pbc_28772 / pbc_29304 上都返回 **0** 张表（这两篇有跨页大表）；pbc_21078 真表在 p4/p5/p6/p8，两者只在 p10/p11 命中（误报）；CCR-18-2728 的 Table 1 在 p4，两者只在 p1 命中页眉框；blood 页对了但把 p2 那一张表切成 9 块。pdfplumber 的 text strategy 退化成"每页恰好 1 张表"。**页面旋转 0/90/270 都试过，救不回来。**
   **根因**：这些期刊用**三线表**（只有顶/中/底三条横线，无竖线）。lattice 靠闭合框线网格切单元格，没竖线就找不到列边界；text strategy 在**整页尺度**上被双栏正文、页眉页脚、参考文献的对齐噪声压倒。
   → 所以"发现"交给 docling。但**给定 bbox 后在框内抽单元格**是另一回事（噪声源已被排除），见 #5b。

6. **关键词常常只写在表格内部、不在 caption 里。** 两种表现，都实测到了：
   - **文字表**：`pbc_28772` 的金标准表 caption 是 `TABLE 1 EPZ011989 as a single agent and in combination for all PDX models`（无 `response`），但**第 11 列表头是 `Obj. Response`**。→ 文字表必须把表格内容一起匹配，而且**不花钱**（过滤时内容已在内存里）。
   - **图片表**：`pbc_24724` Fig.1 的 legend 只有 `objective response`，没有 `PD`/`CR`/`PR`/`SD`（这些在图内的表格里）。→ 这里内容要付 OCR 代价，故走三级梯度。
   对图片做纯文字 OCR **能**认出来（实测该图识出 `CR/MCR/PD1/PD2/PR/SD`），但**很贵**：10.6–28.4 秒/图，比 docling 跑完整篇（8–14 秒）还慢；且实测 8 张图里只有 1 张含目标关键词，无预筛就是 7/8 白跑。→ 必须先用 legend 弱信号词（`response` / `activity` / `objective` / `summary`…）预筛，只对可能是响应数据的图做粗 OCR。

7. **preprint / peer-review 版靠字符串就能测出。** 566455 每页页脚有 bioRxiv 水印，全文 `bioRxiv`×126、`preprint`×189、`peer review`×63。不需要几何判定。

8. **「乱码率」测不出 OCR 文字层 —— 不要用它判断文字层可不可信。**
   乱码率只能测**编码损坏**（替换字符 `�`、私用区字符）。OCR 产出的是**合法 ASCII**，所以：
   - `rstl.1665.0039`（1665 年，ABBYY 扫描识别）乱码率 **0.0%**，但文字层是 `"the Cafe of the Heart"`（`Case`）、`"ufes to Ywim"`（`uses to swim`）、`"from the Bafe"`（`Base`）、`"not fm'ootb"`（`smooth`）—— 古籍**长 s（ſ）被识别成 f** 的经典错误。
   - `EAP in advanced gastric cancer`（2005 年扫描件）乱码率 **0.0%**，文字层是 `"Preusser et al'"`（句点→撇号）、`"Cordia"`（应为 `Cardia`）、一堆 `?`。
   现代期刊 PDF 的文字层确实健康（10 篇实测乱码率 0.00%、2400–5300 字/页），但**"乱码率低"不等于"可信"**。

8b. **判断文字层是否 OCR 派生，用结构信号：某页被单张位图覆盖 ≥95% 宽和高 ⇒ 该页文字层是 OCR 派生 ⇒ 该页所有表无条件 `confidence=low`。**
    实测 2/2 命中、10/10 无误报：两篇扫描件分别是 100%×101%（3/3 页）和 100%×100%（5/5 页）；其余 10 篇最大仅 83%×63%（pbc_29304 p5），有 12 个百分点余量。
    **元数据只能辅助，不能反推**：`rstl.1665.0039` 的 producer 是 `ABBYY Recognition Server`（可用正则 `ABBYY|Recognition|Tesseract|FineReader|OmniPage|ocrmypdf` 命中），但 `EAP gastric cancer` 的 creator/producer **全是空字符串**。其余 10 篇全是排版软件（Distiller / PDFlib / Antenna House）。

8c. **图表可能是「矢量绘制」的 —— 那一页一张嵌入位图都没有。图片路径的候选来源必须是「图区域」，不是「嵌入位图」。**
    实测 pbc_21078 的 `Fig. 3`（p10）与 `Fig. 4`（p11）是矢量热图：该页**位图数 0**、矢量指令 105/112 条、文字层 5896/6075 字符。它们的 legend 明确含 `response`。
    早先按"嵌入位图"找图片候选，结果**两条路都看不见它们**：OCR 路径没有位图可抠；docling 又把它们归成 `PictureItem`（无网格），文字路径也没有候选 → **静默漏表**（是用户人眼核对才发现的）。
    解法（与前身项目 Channel B 一致，那边 45 张 `figure_ocr` 实测有效）：**候选取自 docling 的 `PictureItem` 区域**；该区域被嵌入位图覆盖就抠原生分辨率（事实 #4），否则**渲染该区域**（矢量无损放大，300dpi 足够 OCR）。这样矢量图与位图走同一条路。
    另加一道兜底（铁律 #2）：**caption/legend 命中了关键词、却一张表都没产出 → manifest 必须留失败行**，写明"可能本来不是表格，也可能是矢量绘制/版面识别漏了，请人工看一眼该页"。让"漏了"至少可见。

9. **期刊常把"表格 + 图"拼成一张嵌入图，版面模型会整体标成 `chart` 并跳过表格识别。** 实测 pbc_24724 Fig.1 是一张 1904×1306 的图，左半是完整有框线表（`Xenograft Line | Histology | Log-rank p-value | Median Score | Midpoint Difference | Overall Group Response | Heat Map`，44 行数据），右半是条形图。`PPStructureV3` 把整图判成 `chart`(score 0.79) → `table_res_list` 为空；`TableRecognitionPipelineV2` 直接喂全图同样返回 0。
   **解法：绕过版面分类，用 `TableCellsDetection(model_name="RT-DETR-L_wired_table_cell_det")` 直接在整图上检测单元格，取其外接框裁剪后再还原。** 实测在这张图上检测到 300 个单元格、外接框 `x=1–911 / y=38–1303`（占宽 48%），裁剪后还原出 **45×7**（44 行数据 + 表头，与人眼一致），关键词 `CR/MCR/PD1/PD2/PR/SD` 全部识别到。

9b. **裁剪框只能用「密集单元格带」算，不能用全部单元格的外接框。** 检测器会在**条形图区域也检出伪单元格**（柱子和网格线形成方框），把外接框撑到整图 → 裁剪等于没裁 → 版面模型又把整图判成 `chart` → 返回 0 个表（就是 #9 那个坑，用外接框裁剪时没防住）。
    实测每 5% 宽度的单元格数分布：
    - `pbc_21296` Fig.1：`[47,0,0,48,0,47,47,0,47,47 | 0,0,0,1,0,0,0,1,0,0]` ← 左半表格区每列 47–48 个，右半图区只有 2 个孤立的 1。外接框占宽 **99%**（等于没裁，还原失败）；只留密集带后裁到 ~50%，**还原成功**。
    - `pbc_24724` Fig.1：`[46,0,46,0,47,47,46,47,21 | 0×10]` ← 天然只占 45%，所以它一直是成功的。
    - `CCR` Figure 1：`[0,0,16,0,0,1,0,2,8,10,10,0,10,1,0,10,10,11,0,10]` ← 全宽均匀分布，那是 western blot 面板**不是表**，还原失败是**正确行为**。

9c. **docling 会把一张嵌入位图拆成多个 `PictureItem`，必须按子区域裁剪原生位图。** 实测 `pbc_21296` p3 只有一张位图 `[127,77,463,301]`，docling 给了两个 PictureItem：`[126,75,298,302]`（左半，就是表格）与 `[311,85,463,269]`（右半，条形图）。
    忽略子区域、每次抠整张位图的后果：两个候选拿到同一张全图 → **同一张表被抽两遍**（实测出现过 `fig_1` 与 `p03_t3` 两份 `45×6`）。
    按子区域裁剪同时比 #9b 的密集带启发式更精确 —— docling 已经替我们分好区了，密集带只当兜底。

9d. **低分辨率图在还原前要放大，但必须先裁对区域。** 目标 ~300 有效 dpi、最多 4×（Lanczos）。
    实测 `pbc_21296` Fig.1（560×374、有效 120dpi）**缺一不可**：
    - 只放大不裁剪 → 1×/2×/3×/4× **全部还原失败**（整图被判成 chart）
    - 只裁剪不放大 → 出 `44×5`，表头全空、`Rhabdoid` 读成 `Ahabdad`、`-2.0` 读成 `-210`
    - 裁剪 + 放大 → `45×6`，表头 `…|Histology|Score|Difference|Group Respons|Map`、`-2.0` 正确
    放大不增加信息，只是让 OCR 模型达到它期望的输入尺度。

10. **PaddleOCR 表格管线必须强制英文识别模型。** `lang='en'` 这个参数**不被接受**（`ValueError: Unknown argument: lang`），要用 `TableRecognitionPipelineV2(text_recognition_model_name='en_PP-OCRv5_mobile_rec')`。不指定的话中英混合模型会把 `-3` 识别成中文字符 `心`，还会把 `Histology` 读成 `Hiatology`、`Median` 读成 `Mdan`。

11. **OCR 会把 `<` 误读成 `-`。** 实测 `<0.001` → `-0.001`。这是**最危险的一类错误**：结果是一个看起来完全合法的数值，不会触发任何格式告警。统计值的比较符号一律不可信，必须人工核对。（即使强制英文模型，`Overall Group` 仍被读成 `Overal Croup`、`Rhabdoid` 读成 `Rabdoid`。）

12. **跨页续表有两种，必须两条判据都实现，只做一条会漏。**
    - **写 `(Continued)` 的**：pbc_21078 p5 = `TABLE I. ( Continued )`、pbc_28772 p4 = `TABLE 1 (Continued)`、pbc_29304 p4 = `TABLE 1 (Continued)`。注意实测出现过 `( Continued )` 这种括号内带空格的写法。续表自己带标号，所以天然落在 #2 的 CAPTION 判定里。
    - **caption 完全为空的**：pbc_26870 的 Table 1 跨 p18–p19，p18 有 caption `Table 1 Summary of in vivo Activity of Patritumab…`，**p19 的 caption 是空字符串**。此时唯一的线索是**列名逐字相同**（实测两页都是 `Tumor Line | Treatment Group | Median Time to E | P-value | EFS T/C | Tumor Volume T/C | EFS Activity | Response`，8 列全一致）。

13. **docling 自己的 caption 关联经常失败，必须用 #2 的判据自己补。** 实测 pbc_30017 p7 与 pbc_26870 p19 的 `TableItem.caption_text()` 都返回空字符串，而我们的行首正则在同一页能找到真 caption（pbc_30017 p7 有 `Table 1`）。另外 pbc_29304 有 8 个 `PictureItem`、其中 7 个 caption 为空。

14. **文字表里的 `<` 是可信的，OCR 出来的不可信。** 实测 pbc_21296 的文字表正确保留了 `< 0.001`、`> 3.1`、`> EP`；而同样的 `<0.001` 经 OCR 会变成 `-0.001`（见 #11）。这是"文字表 `medium/high` vs OCR 表无条件 `low`"这条分档的直接依据。

15. **合并单元格（合法）与行被压掉（bug）在 CSV 里长得一样，都是"有空格子"，靠字符总量区分。**
    合法的例子：pbc_30017 的 `Model` 列，`OS-2` 在原表纵跨 Control / Copanlisib 两行，导出后第二行该格为空 —— **数据没丢，只是位置合并**。
    bug 的例子（前身项目的 `merged_row`）：识别器把两个数据行挤进一格 —— **总字符数明显少于该区域文字层的字符数**。
    → 所以判据是**内容覆盖率**（抽出的单元格总字符数 ÷ 该区域文字层字符数），不是"有没有空格子"。
    **⚠ 这条判据实测拦不住真 merged_row**：`pbc_21296 TABLE I` 第 41 行把两行挤进一格，
    但字符**一个都没丢**（只是放错位置），覆盖率毫无异常，最终标成 `high`/`notes=''`。
    有效的判据是**数值列里出现多个数**（`eval/checks.py detect`），见 `eval/findings.md` F-001。

16. **docling 把多级表头用 `.` 连接各层拼平。** 实测 `Bao et al. 2025`（留出集）原表是两层表头
    `Training` / `Cancer` + `n =2,254`，导出成一个格子 `Training.Cancer.n =2,254`。
    这是 docling 的既定行为、不是 bug，但**任何拿单元格内容去跟文字层做字符串比对的地方都要知道**
    —— 拼出来的整串在文字层里不存在，会被误判成抽错。
    **证据基础只有留出集 1 篇**：开发集 `0.pdf_input/` 那 12 篇**一个多级表头样本都没有**，
    所以这条在开发期从未暴露。

17. **文字层比对必须按「表所在的页」，不能按全文档。** 实测 `pbc_21078 fig_3`（矢量热图）
    按全文档比 **92.6%** 命中、看起来很干净；按本页只有 **56.1%**；而它的标签
    （`BT-29`/`KT-16`/`KT-13`/`Rh18`/`Ewing`）在 p10 的 word span 里**一个都不存在**
    —— 那些是矢量轮廓，不是文字。同一个词在论文别处出现（正文提到 `rhabdoid`、`neuroblastoma`）
    就足以让 OCR 垃圾"验证通过"。
    按页 + 去控制字符 + NFKC 拆连字之后，判别力是**文字表 98.6–100% vs 图片表 24.6–56.1%**。
    两条归一化缺一不可：`pbc_21078` p6 文字层里是 `ﬁnal RTV`（U+FB01 连字）而抽出的是
    `final RTV`；`table_ii`/`table_iii` 各有 14 个 `\x01`（符号字体的对钩类字形）。

18. **docling 会把 PDF 里未映射的**字形名**原样写进单元格。** 实测留出集 `Medina 2025`
    的缩进用 em space，导出成 `/emspaceMean` / `/emspaceRange` / `/emspaceI`（27 处），
    `Chang_2023` 有 1 处 `/bowtie`。**开发集 12 篇零出现**，是留出集补上的一档。
    清理时**只能认已知的空白字形名、且必须保留后面的内容** —— 贪婪匹配 `^/[a-zA-Z]{2,15}`
    会把 `/emspaceClear cell`（卵巢癌组织学分型）整格吃空，那是丢数据。
    见 `rules.strip_glyph_names`，在 `engine_docling._df_to_rows` 里就地清掉。

## CLI 契约

已装成 console script（`pip install -e . --no-deps`），任意目录可用：

```bash
conda activate gemini
pdf-table-extract PAPER.pdf --list                       # 先看有哪些表，几乎免费
pdf-table-extract PAPER.pdf -k survival -o results/      # 抽
```

| 参数 | 含义 |
|---|---|
| `PDF` | 位置参数，输入 PDF |
| `--list` | **不抽内容**，只列出全部标号 + 完整 caption/legend + 页码。不跑任何 ML。给 `-o` 时另写一份机读的 `captions.csv` |
| `-k, --keyword` | 可重复。**默认 OR**：命中任一即导出 |
| `--match-all` | 改成 AND：必须全部命中 |
| `--keywords-file` | 从文件读关键词，一行一个 |
| `-o, --outdir` | 输出**根**目录，默认当前目录 |
| `--prefix` | 本篇的子文件夹名，默认取 PDF 文件名（会清理空格/尾点等） |
| `--pages` | 限定页范围，如 `3-8,12` |
| `--no-ocr` | 跳过一切 OCR（只处理文字表，快速试跑用） |
| `--dpi` | 兜底渲染分辨率，默认 200（优先抠原生图，见 PDF 事实 #4） |
| `--dump-images` | 同时导出表格原图 PNG，便于人工核对 |
| `--all` | 忽略关键词导出全部；**也是标号惯例不被识别时的逃生舱**（跳过标号匹配，直接用版面分析找表）。不会关掉单元格闸门 |
| `--label-pattern` | 覆盖默认标号正则（核心部分，不含锚点） |
| `--hint-words-file` | 用户自备的领域词表（OCR 闸门放行用），**默认空** —— 领域知识的注入点 |
| `--allow-preprint` | 覆盖 preprint 拒跑（见铁律 #3） |

**匹配语义：默认「全词 + 英文屈折变体」，不区分大小写。** `--substring` 可退回纯子串。

为什么不用子串（实测）：2–3 字母的关键词在子串模式下全是噪声 ——
`PR` 命中 **130** 处（`rep`**r**`esentative` / `P`**r**`exasertib` / `P`**r**`otein`）、
`CR` 命中 **41** 处（大量来自 `vin`**cr**`istine`）。`CCR-18-2728` 那三张图（该篇**没有**响应码表）
在子串模式下会被 `CR`/`PR`/`PD` 全部误命中，全词模式下全部正确排除。

为什么必须带屈折变体（实测）：**纯全词会漏** —— `Demographic` 全词命中 **0** 处
（原文是 `Tumor Demographics`）、`xenograft` 全词 11 处 vs 带变体 **38** 处。

覆盖的形态（纯英语构词法，不含领域知识）：`+s` `+es` `y→ies`（`toxicity`→`toxicities`）、
`+d` `+ed` `+ing`、`e→ing`（`dose`→`dosing`）、末辅音双写（`control`→`controlled`）。
含数字的关键词（`PD1`、`S2`）**不做形态展开** —— 那是编号代号，没有屈折。
多词短语只对最后一个词展开。

**匹配范围按路径不同（这是修正过的决定）：**

| 路径 | 匹配什么 | 为什么 |
|---|---|---|
| **文字表** | caption + legend **+ 表格内容（全部单元格）** | **不花钱。** docling 是整篇跑一次、过滤发生在抽取之后，所以过滤时单元格内容早就在内存里了 |
| **图片表** | 只有 caption + legend；未命中时再走三级梯度（单元格闸门 → 纯文字 OCR → 结构还原） | 图里写了什么必须先 OCR 才知道，那才是真花钱的地方 |

**为什么修正**：早先定的是"只认 caption+legend"，那是因为我把"匹配表内内容"整体判成了昂贵操作 —— **对文字表这个判断是错的**。代价是漏掉了明显该命中的表：实测 `pbc_28772` 的金标准表 caption 是 `TABLE 1 EPZ011989 as a single agent and in combination for all PDX models`（无 `response`），但它**第 11 列的表头是 `Obj. Response`**。`-k response` 本该命中它。

manifest 的 `matched_on` 列记录靠哪儿命中：`caption` / `cell` / `caption+cell` / `image_text`。

**输出布局：一个 PDF 一个文件夹** —— `<outdir>/<prefix>/`，里面是每表一个 CSV + `manifest.csv`。

**manifest 是累加式的**：同一篇 PDF 换关键词反复搜，**追加行、不覆盖历史**。`query` 列记录那次搜的检索条件（也是去重键 —— 同一条命令重跑是更新而非堆积）。所以"用 a 搜到 table1、用 b 也搜到 table1"就是两行，各带自己的 `query` 和 `matched_on`。

**刻意不按关键词自动分文件夹。** 同一张表常被多个关键词命中（分文件夹会产生重复 CSV，之后分不清哪份准）；一次运行可带多个关键词（文件夹名不好定）；关键词可能含空格/斜杠（路径不安全）。累加式 manifest 已经把"哪次搜到了什么"记全了，不需要靠目录结构表达。要把不同次运行**物理**分开仍可用 `--prefix`。

## 输出契约

输出目录里每张命中的表一个 CSV，命名 `p{页码:02d}_{table_id}.csv`（如 `p05_table3.csv`），加一份 `manifest.csv`：

| 列 | 说明 |
|---|---|
| `table_id` | 表标识，尽量用原文编号（`table3` / `table_s1`） |
| `label` | 原文标号原样（`Table 3` / `TABLE I` / `Supplementary Table S2`） |
| `page` | 1-based 页码 |
| `caption` | 抽到的 caption/legend 原文；抽不到留空 |
| `csv_path` | 相对 outdir 的路径；**失败时留空但行必须存在** |
| `extractor` | 实际用了哪个引擎 |
| `source_type` | `text` / `image` |
| `matched_on` | 凭什么命中：`caption` / `legend` / `image_text` |
| `matched_keywords` | 命中的关键词，`;` 分隔 |
| `n_rows` / `n_cols` | 形状，便于一眼看出还原是否崩了 |
| `confidence` | `high` / `medium` / `low` |
| `grid_status` | `ok` / `grid_mismatch` / `fallback_raw` |
| `notes` | 人可读的告警（`merged_row?` / `spans_pages=5-6` / `possible_continuation` / `caption_detached`） |

**confidence 判定（硬规则，不要自创第四档）：**

- `high` —— 两个独立引擎的行列数一致 **且** caption 归属校验通过
- `medium` —— 单引擎得到的网格，自洽但无第二意见
- `low` —— **任何** OCR 派生的结果，无条件 low，不管看起来多干净

## 铁律

前四条来自前身项目（`../02_extract_ref/` 的三份 handoff）用真实 bug 换来的教训。

1. **一个网格在被信任高于原文之前，必须先验证它确实属于它所声称代表的那张表。** 前身项目最痛的 bug：上游把 Table S1 的网格错配给了 Table 2，而 prompt 无条件命令"网格是权威、绝不用原文覆盖网格"，于是忠实地抽出了一张错误的表。做法：拿 caption 里的实词 sanity-check 网格表头/首列，对不上就标 `grid_mismatch`、降级、在 `notes` 写清 —— 而不是静默采用。

2. **不静默丢表。** 抽失败的表在 manifest 里也要有一行，`csv_path` 留空、`notes` 写失败原因。silent drop 会让人误以为"这篇文献就没有相关表"。

3. **不静默返回空。** 全文扫不到任何标号、或判定为 preprint → **报错退出**，不返回空结果。
   注意「正文与图表分离」**不是**拒跑理由：实测 pbc_26870（表在 p18–19）、pbc_30017（表在 p7）虽然图表都挪到了文末，但 **caption 依然紧贴着它的表**，配对照常成立。拒跑只对 preprint（页脚字符串检测）生效；个别标号配不到实体只标 `caption_detached` 红旗跳过那一张，不整篇拒跑。

4. **OCR 结果无条件 `confidence=low`。** 表格结构识别器会把相邻行合并成一格 —— 在 `notes` 标 `merged_row?`。图片表永远假定需要人工过一眼。

5. **降级要可见。** 换了引擎、走了兜底，必须体现在 `extractor` / `grid_status` 里，不要让用户以为都是同一种质量。

6. **宁可多抽不可漏抽。** 关键词默认 OR 就是这个取向 —— 多抽的代价是删文件，漏抽的代价是重跑整套 OCR。

7. **文字层优先。** 只有文字层不健康的页才 OCR。整本强制 OCR 又慢又不准。

## 测试料

`0.pdf_input/` 下有 7 篇真实文献，每篇的预期行为记在同目录的 `pdf_for_testing.txt` 里，**改动后必须逐篇回核**：

| PDF | 考什么 | 已实测的正确结果 |
|---|---|---|
| `pbc_28772` | 跨页大表 | `38×11` + `6×11 (Continued)` = **44 行**（前身项目的金标准） |
| `pbc_29304` | 跨页 + 横向表 | 需转正：`17×15` + `24×15 (Continued)`。不转正会退化成 `15×16` + `1×2` |
| `pbc_21078` | 横向表 + `PD`/`CR` 的位置 | 需转正：`43×9` + `16×8 (Continued)`；`TABLE II` `31×10`、`TABLE III` `35×10`。**注意其 `PD`/`CR` 在文字层，不在图里**（原标注有误） |
| `pbc_24724` | 图片里的表（核心用例） | 文字表 `27×6` / `41×10`；Fig.1 图内的表需裁剪后还原成 `45×7`，含 `CR/MCR/PD1/PD2/PR/SD` |
| `1078-0432.CCR-18-2728` | supplementary 不在 PDF 里 | 真 caption 只有 `Table 1` (`28×5`)；8 个 supp 标号全是引用 → 应提示去下载 supp |
| `blood_2014_12_518900` | 较复杂的实验设计 | `17×7` / `14×7` / `6×7`（Table 4 只被引用，不在 PDF 里） |
| `pbc_26870` | 正文与图表分离 + 横向 + **续表 caption 为空** | 需转正：p18 `26×8` + p19 `18×8`，p19 caption 空、靠列名一致判续表 |
| `pbc_30017` | 正文与图表分离 + **合法合并单元格** | p7 `12×11`（竖排仅 4 行，正确地不转正）；`Model` 列有纵向合并 |
| `pbc_21296` | 横向表（竖排 446 行）+ 3 张 120dpi 高糊图 | 需转正：p4 `43×10`，且 `< 0.001` / `> 3.1` / `> EP` 都正确保留 |
| `EAP in advanced gastric cancer.` | **扫描件 + 出版社 OCR 文字层** | 3 页全被位图 100%×101% 覆盖 → 强制 `low`。docling 能从 OCR 字符坐标恢复结构：p2 `21×3`，列名 `['', "Preusser et al'", 'Taal et al (n =']`，行 `['Male + female','49 + 18','21 + 5']`（原 OCR 文字流是挤在一行的）。三关键词都不命中，需 `--all` |
| `rstl.1665.0039` | **真扫描件（1665 年 Phil. Trans.，ABBYY）** | 5 页全被位图 100%×100% 覆盖；**caption 判据 5 页全零标号** → 必须报错喊停并指向 `--label-pattern` / `--all` |
| `566455` | **negative control** | bioRxiv preprint，必须早期报错说明是 peer-review 版本（且要发生在关键词匹配**之前** —— `Demographic` 会命中它的 `Table S4`） |

**回归门槛**：任一篇出现"manifest 行数 < 命中张数且无对应失败行"即为 bug。

### 语料事实：同义用词（**不要写成内置同义词表**）

用户人眼核对给出的 ground truth，用来判断"0 命中"是真没有还是词表不全：

| PDF | 它的 Table 1 实际是什么 | 论文用的词 | 后果 |
|---|---|---|---|
| `pbc_26870` | **是 PDX 表** | 表内写 `Tumor Line`；**caption 就是 `Table 1` 三个字、无任何描述文字** | `-k xenograft -k PDX` → 0 命中。`-k "tumor line"` 能抓到 |
| `pbc_30017` | **是 PDX 表** | 写 `osteosarcoma models`；caption 是 `Table 1: Copanlisib as single agent vs. control (N=10/agent)` | 同上 0 命中。`-k model` 能抓到但该词太泛 |

**这不是工具缺陷，是词表不全** —— 按泛用性原则，`PDX = xenograft = tumor line` 这类等价关系必须由用户经 `-k` / `--keywords-file` 提供，工具不内置。

但**工具有义务让这种漏可见**：0 命中时必须列出本篇实际有哪些表/图（`emit.print_zero_hit_hint()`），否则用户会误以为这篇没有相关内容。`pbc_26870` 那条 `Table 1（caption 无描述文字）` 一行就解释了为什么没命中。

## 留出集 `holdout/` —— 只验阈值，边界很窄

```bash
bash holdout/run.sh     # 几秒/篇，跑完直接给结论
```

**一句话职责**：拿一批**从未参与开发**的论文，验证在 `0.pdf_input/` 那 12 篇上定的**阈值**
还成不成立。语料是 `/Users/funanhe/Downloads/Zotero_Precancer_PDFs/`（25 篇现代论文，
2020–2025，非 PDX 领域）。

### 边界（刻意画得很窄，不要越界）

| | |
|---|---|
| **做** | 只跑 `--list`，检 4 个阈值判据 |
| **不做** | **不跑抽取、不验"抽出来的表对不对"** —— 那是 `eval/` 的事 |
| **不需要** | ground truth —— 那 25 篇没人逐张核对过 |
| **不依赖** | `eval/` 的任何代码。`holdout/` 只 import `pdf_table_extract`，自包含 |

`holdout/` 只有两个文件：`run.sh`（扫语料）和 `report.py`（阈值体检）。

### 它是 test set，不是 validation set

**只在"我说做完了"的时候跑，跑完不许拿它调参** —— 否则它退化成第二个开发集，
就再也不能当泛化证据用了。

> ⚠ **已污染过一次（2026-07-26）**：`eval/` 的表头豁免判据改过两次，都是看到
> `Bao 2025` / `Bruhm 2025` 的误报之后才改的。改的是 eval 判据、不是产品代码，性质轻一些，
> 但**这两篇往后不能再当独立证据用**。见 `log.md` §20。

### 验收区间必须按「人核实过的正确值」定，不能按「观测值」定

这条是原则，不是某次的实现细节。

实测教训：`report.py` 第一版把"转正页"的区间设成 `0–3`，因为跑出来就是 3，于是永远 ✓ 通过。
**但那 3 页里有 2 页是已核实的误报。** 照观测值定验收线 = 这个体检永远不会失败 = 等于没装。
改成按人核实的正确值（真横向表只有 1 页）之后，它才正确地报出失准。

`EXPECT` 里每一项都要写清核实依据；换一批语料时必须重新人核，**不能照抄**。

报告分三档：`✓` 通过 / `▲` 已知问题（绑到 `eval/findings.md` 的编号，修好自动转绿）/
`✗` 新失准。**只有新失准才 exit 1** —— 已知问题天天亮红灯会让人对红色脱敏。

### 检得到 / 检不到（别把"跑过了"当成"验过了"）

**检得到**：标号数量级、`VERTICAL_LINES_THRESHOLD` + `VERTICAL_RATIO_THRESHOLD`（转正，事实 2c）、
`FULL_PAGE_IMAGE_COVER` + `OCR_LAYER_MIN_CHARS`（OCR 文字层，事实 2d）、
`PREPRINT_MIN_PAGE_RATIO`（preprint，事实 2e）。

**检不到**：

- **图片路径的任何东西** —— `--list` 不跑抽取
- `CROP_PAD_RATIO` / `MIN_RIVAL_OVERLAP` / `MIN_IMAGE_COVER_H` —— `--list` 根本不经过
- **标号漏检** —— 这是最大的盲区。没有 ground truth，漏了只显示成"这篇表少一点"，
  **看起来完全合理**。（2026-07-26 用 `spike/probe_zero_table_papers.py` 做过一次非循环排查：
  对比「行首规则找到的标号」与「全文任意位置出现的 `Table N`」，差集逐条读 —— 见 `findings.md` F-003）

### 一条推理陷阱：分母不对，比例就是假的

**"跑了留出集、没命中"不等于"这个问题不普遍"**，还要看这批料里**有没有该问题出现的场景**。

实测：某个"两行压一格"的 bug 在留出集 27 张表上 0 命中，看起来像个案。
但留出集里真横向转正表**只有 1 张**（还只有 3 个数据行）—— 分母根本不够，
"没命中"什么都说明不了。见 `log.md` §19。

## 端到端回归（改代码后必须跑）

```bash
conda run -n gemini python -m pytest tests/ -q   # 单测：纯逻辑、0.05s、不碰 PDF
bash eval/regression.sh                          # 快档 ~2 分钟，改一次代码跑一次
bash eval/regression.sh full                     # 全量 ~7 分钟，一轮改动做完再跑
```

**期望值不写在这里，写在 `eval/expected.csv`（机器读的那份）。**
早先这里有一张 42 行的手抄基线表，跟 `expected.csv` 是同一批数字的两个副本 ——
**结果漂了 2 处**（`resp_24724/p05_fig_1` 文档记 45、实际 47；`resp_30017` 文档记 `matched_on=cell`、
实际 `caption`），而且很久没人发现。两份手维护的同一批数字必然漂移，所以只留一份。

三条规则（这些一次实验推翻不了，所以留在本文件）：

1. **断言的是「行为没变」，不是「行为正确」。** `expected.csv` 是**现状快照**，里面有条目
   钉着**已知错误的**当前值 —— 见 `eval/known_issues.md`。这样断言器才能全绿、才起得到
   "防改坏"的作用。修 bug 时把条目从 `known_issues.md` 挪进 `expected.csv` 并改数字。
2. **它唯一不可替代的能力是查「本该出现的表消失了」。** 单测不跑流水线；`eval/checks.py detect`
   只遍历已存在的 CSV；人眼看一屏数字最容易漏的恰恰是"少一行"。而本项目由用户发现的 3 个 bug
   **全是**表悄悄没了（`log.md` §5）。这是它存在的根本理由。
   （原话是「本项目没有 git，改坏了没 diff 可看」。**2026-07-30 起有 git 了**，
   但理由只是被削弱、没有消失：diff 只能告诉你**代码**改了什么，
   告诉不了你「这一改之后某张表不再出现了」—— 那要跑一遍才知道。）
3. **分快慢两档是因为 OCR 太贵且收益低。** 实测全量 424 秒（12 条跑 OCR）vs 快档 121 秒（0 条）。
   OCR 路径的产出本来就是 `confidence=low`、本来就要人核（铁律 #4），钉住它收益接近零。
   快档覆盖文字表的**全部**路径：跨页拼接、横向转正、合并单元格、出版社 OCR 文字层、
   两个拒跑退出码、"该 0 张的不许出表"。

**回归门槛**：任一篇出现"manifest 行数 < 命中张数且无对应失败行"即为 bug；`eval/checks.py regression` 必须全绿。

## 从前身项目 handoff 里继承的两条实测记录

（`../02_extract_ref/2026-07-24-handoff-consume-stage01-rework.md`，值得回查）

1. **`pbc_28772 Table 1` 在前身项目里是弱表**：caveat 4 记「gold 文章，A 错行、B 转置都不理想，**保留 A**」（A=pdfplumber，B=docling）。本项目用 docling + 转正后一次得到正确的 44 行数据，**比当初好**，所以它才能当金标准用。
2. **同页多表时版面模型会选错**：caveat 5 记「少数 `Table I` 块实际指向 Table II / Supplemental 表（同页多表、Docling 按响应码密度选错；pdfplumber 选对）」。本项目遇到的同类 bug 是 blood p4/p6 上 `Figure N` 的图注被配给了 `Table N` 的网格 —— 修法是**配对必须按 kind 分开、table 优先**。

## 已知限制

1. **低分辨率图片表的结构还原会失败。** 实测 `pbc_21296` Fig.1（120dpi）闸门检出 285 个单元格、`CCR` Figure 1 检出 99 个，但 `TableRecognitionPipelineV2` 都还原不出表格。此时按铁律 #2 在 manifest 留失败行（`检出 N 个单元格但结构还原失败`），不静默丢弃。
2. **`pbc_26870` 的 Table 1（p18 `26×8` + p19 `18×8`）没有被三关键词覆盖** —— 它的 caption 不含 `response`/`xenograft`/`Demographic`，只有 `--all` 才测到。它是"续表 caption 为空、靠列名一致拼接"的唯一样本，改拼接逻辑时要专门用 `--all` 回归它。
3. **竖排方向 `(0,1)` 的 270° 分支：触发过 4 次、4 次全是误报，正例仍然 0 个。**（2026-07-26 更新，原记为「未验证」）
   阈值验证集里 51 篇含 `(0,1)` 竖排，其中 4 页真的触发了该分支 —— `4H69WJ8B` p4（1767 行，多面板图轴标签）、`5D5JKFJM` p17 与 `5PMEJG7T` p6/p9（各 71 行，`©2017…` 版权声明被逐字符拆行）。加了事实 2c 的第三条判据后**全部消除**。
   **但仍然没有一个真横向表是 `(0,1)` 方向的** —— 所以 `270°` 这个返回值至今没有正例验证过。
4. **枚举式引用只取第一个标号** —— `Tables IV and V` 只会记下 `table iv`。
5. **连字符按「词字符」处理，所以 `PR-104` 不满足 `-k PR`。** 这条最初选反了（我按"先保召回"选了边界），是实测数据纠正的：语料里所有连字符 token **全是标识符** —— `PR-104`/`PR-104A` 是药名（blood 那篇整篇讲这个药，边界规则下 `-k PR` 会命中该篇每一处）、`NB-SD` 是移植瘤系名、`ALL-11`/`KT-13`/`CTG-0241`/`JNJ-26481585`/`BCP-ALL` 全是编号代号；而"值得抓的连字符复合词"（如 `dose-response`）**一个都没有**（语料里原文是空格写法 `Dose response curves`，且曲线图本来就不是要的表）。零召回损失、消掉一整类误报，顺带 `PD-L1` 也不再满足 `-k PD`。
   单测 `test_hyphen_counts_as_word_char_not_boundary` 把它钉住。
   **反面代价**：真出现 `dose-response` 这种连字符复合词时会漏。若将来语料变了，改回边界只需一行，但要先量一遍连字符 token 都是什么。
   **补充（2026-07-30，PPTP 语料实测）**：边界正则是 `(?<![\w-]){body}(?![\w-])`，
   lookaround 只排除 `\w` 和 **ASCII 连字符**。**U+2013 短破折号不在其列，所以会命中** ——
   `pbc_22921 TABLE III` 的 `Dose–Response`（实测确认是 U+2013）被 `-k response` 正确命中。
   对照：`'Dose–Response Activity'` → `True`，`'dose-response curves'` → `False`。
   ⇒ 上面那个"会漏"的担心，**在真实排版里多半不会发生**，因为期刊用的是短破折号。

6. **累加式 manifest 不能并发写。** `emit.write_manifest()` 是"读旧文件 → 去掉同 `query` 的行 → 写回"，**没有加锁**。同一个输出目录上并行跑两条命令会互相冲掉对方的行。串行没问题（这就是 `eval/matrix.sh` 里三组关键词串行的原因）。要并行请用不同的 `--prefix` 分到不同文件夹。

7. **图片路径的第二意见只有形状**（单元格检测器的行列带数 vs 还原结果），没有逐格校验；OCR 错字只能靠 `confidence=low` + `--dump-images` 人工核。

## 三关键词测试模式

刻意用程序完全不知道的词，杜绝预设。**图和表一起搜**（不提供 `--tables-only`，图里的表是核心需求）。

| 组 | 关键词 | 测什么 |
|---|---|---|
| **response** | `-k response` | 流水线跑通；文字表靠 `cell` 命中为主，图片表走三级梯度 |
| **xenograft** | `-k xenograft -k PDX`（OR，命中任一即算） | 文字路径主力 + 横向表转正。**必须带 `PDX`** —— `pbc_28772` 的金标准表 caption 是 `…for all PDX models`，只搜 `xenograft` 抓不到 |
| **Demographic** | `-k Demographic` | 精确性：应**只**出 `pbc_21078 TABLE I` 一张，多出即误报 |

同义词/缩写由**用户**提供（泛用性原则：工具不内置 `PDX = xenograft` 这类知识），落地形式就是多个 `-k` 或一个 `--keywords-file`。

### 已决定：`response` 组**不加** `PD` / `CR` / `PR` / `SD` / `MCR` 这类响应码

（2026-07-25 用户拍板）

**拒绝理由是"选择性不够"，不是"技术做不到"** —— 这个区分要写清，否则后人会在错误前提下重开讨论：

- 技术上**已经安全**：这些是 2–3 字母词，在早先的子串匹配下是灾难（`PR` 命中 130 处英文单词内部、`CR` 41 处来自 `vincristine`）。换成「全词 + 屈折变体」并把连字符按词字符处理后，`PR` 降到 8、`CR` 降到 2，`PR-104`（药名）也不再误命中。所以**障碍已经消除了**。
- **但它们做筛选词太不挑**：`PD`/`SD` 在语料里出现得太广，加进去会把大量无关表拉进来，等于没筛。`SD` 还兼任"标准差"（实测有 `mean ± SD`）。
- **响应码这一层留给下游**：拿到 CSV 之后按"至少含 N 个响应码"筛更准 —— 数单元格是**精确计数**，在 caption+内容拼成的文本 blob 上数是**近似猜测**。这也是用户提 `*3` 阈值时的判断，成立。同理，`A 且 (B 或 C)` 这种表达式语言也不做进本工具。

尚缺的测试料：竖排方向为 `(0,1)` 的横向表（现有全是 `(0,-1)`，转正规则按对称性推断为 `rotation=270`，**未验证**）。

## 打包 / 发布

开发期用 conda，**发布到 GitHub 时**再补可移植性文件，不要提前做：

- `pyproject.toml`：声明上表里的库为依赖，Python `>=3.11`
- `README.md`：显著位置写清 `paddlepaddle` / `docling` 是重量级依赖（几个 GB），给 Apple Silicon 安装注意事项
- console script 入口 `pdf-table-extract`
- 首次运行会自动下载模型 —— 要在 README 说明，否则用户以为卡住了

## 现状

尚未写代码。技术路线待探针（见 plan）实测后确定。
